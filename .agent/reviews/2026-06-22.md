# Nightly Review — 2026-06-22

_Transcript scan note: The session path `C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container (third consecutive run with this mismatch). Only one JSONL was found — the current session with no prior turns. Analysis is based on code delta since the Jun 21 review, git log, and in-repo artifacts. See "Notes" for the path-mismatch action item._

_Activity since Jun 21: 3 commits across PR #4 (custom exercises feature — migration, API routes, UI, contract, tests) + smoke realignment. ~31 source files touched. Feature complete and merged._

---

## Proposed CLAUDE.md Additions

- [ ] **`status.md` and `decisions.md` are Odin-workspace files, not repo files** — CLAUDE.md step 4 says "Read `project.yaml`, `status.md`, `decisions.md` — project state." These don't exist in the GymTracker repo root; they live in the parent Odin workspace. An agent searching this repo will not find them and may error or invent a path. Add a clarification: "These live in the Odin workspace root (one level up from the repo), not inside `GymTracker/`. Don't search the repo root for them."

- [ ] **Plan files belong in `docs/plans/`, review logs in `docs/reviews/`** — The calendar TZ feature followed this convention (`docs/plans/bugfix-*.md`, `docs/reviews/calendar-*-review-*.md`). The custom exercises feature left `PLAN.md` and `PLAN-REVIEW-LOG.md` at the repo root, where the generic names will conflict with the next feature. Add to the Architecture/Docs section or grill-with-docs workflow: "Implementation plans → `docs/plans/<feature>.md`. Grill-with-docs review logs → `docs/reviews/<feature>-review-YYYY-MM-DD.md`. Do not use generic root-level `PLAN.md` / `PLAN-REVIEW-LOG.md`."

- [ ] **Smoke test coverage rule for new private routes** — Every new route added to `src/routes/api.*.ts` that uses `privateMethod` should get a 401 `assertUnauthorized` entry in `scripts/smoke.ts`. The `/api/exercises/custom` route (GET/POST/PATCH/DELETE) added in PR #4 has no smoke entry. Add to the Testing section: "New `privateMethod` routes must include a `{ expectStatus: 200 }` + `assertUnauthorized` smoke target in `scripts/smoke.ts`."

- [ ] **Graphify trigger: treat 49 stale commits as critical** — The graphify trigger threshold in CLAUDE.md says "≥5 files." The graph hasn't been updated since the exercise-GIFs commit (49 commits ago), and CLAUDE.md currently says "Always use the graph before raw search." That instruction is actively misleading. Either: (a) add a note that the graph must be refreshed before use on a feature involving `exercise-library/`, routes, or DB schema, or (b) add an explicit blocklist of stale areas. Minimum: flag the graph as stale until `graphify update .` is run. Carry-over from Jun 21, now more urgent.

- [ ] **Codex CLI is unavailable in remote sessions** — `PLAN-REVIEW-LOG.md` documents `codex: command not found` for the second major feature in a row (calendar TZ in Jun 21, custom exercises Jun 22). Add a note to the Shell section (or the grill-with-docs workflow): "The `codex` CLI is not installed in the remote container. Grill-with-docs Act 2 (cross-model loop) must be substituted with an isolated-context agent review using the Agent tool. Document this as the canonical remote fallback."

- [ ] **Image upload file size validation is missing** — `CustomExerciseForm.tsx` routes gif vs. image by MIME type but doesn't enforce a size cap. The locked PLAN said "Validate type (image/* incl. gif) and size client-side." The size check was not implemented. Add to the Styling/UI section or custom-exercise docs: "The `exercise-images` upload must client-side-reject files over a max size (Supabase free tier default is 50 MB; recommend ≤5 MB for images/GIFs). The `<input accept='image/*'>` enforces type at the OS picker level only — validate `file.size` before calling `uploadCustomExerciseImage`."

## Memory Updates Needed

- [ ] **No `memory/` directory in the remote container** — Same as Jun 21. `~/.claude/projects/-home-user-GymTracker/memory/` doesn't exist. If memory files exist on the user's Windows machine they are not accessible to remote sessions. Memory audit cannot be performed in this environment.

- [ ] **Custom exercises feature shipped (PR #4 merged Jun 22)** — If a memory entry tracked the "add custom exercises" backlog item, it should be updated to reflect completion.

- [ ] **Graphify critically stale — 49 commits** — Carry-over from Jun 21 (was 18 days then). If a memory entry says the graph is current, update it. The graph now misses: custom exercises domain (`exercise-library/custom-exercise.ts`), the custom API route (`api.exercises.custom.ts`), the `exercise-images` storage bucket, the `created_by` / `archived_at` DB columns, and all changes from PR #5 (calendar TZ + prefill).

- [ ] **`PLAN.md` / `PLAN-REVIEW-LOG.md` at repo root should be moved** — These are the custom-exercises planning artifacts. They'll shadow any future feature plan if left at root. Recommend moving to `docs/plans/custom-exercises.md` and `docs/reviews/custom-exercise-plan-review-2026-06-22.md` in a follow-up cleanup commit. Flag in memory if tracking this.

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow still missing** — Flagged Jun 21, still not created. The process has now run for three features (perf audit, calendar TZ, custom exercises). Create `.agent/workflows/grill-with-docs.md` documenting:
  - Act 1: write plan, adversarial review in ≤MAX\_ROUNDS rounds, REVISE/APPROVED loop.
  - Header format: `MAX_ROUNDS=5`, `PLAN_FILE=docs/plans/<name>.md`, `LOG_FILE=docs/reviews/<name>-review-YYYY-MM-DD.md`.
  - Remote fallback for Act 2 when `codex` CLI is unavailable: use `Agent` tool with `isolation: worktree` for an isolated-context review pass, record as "Substitute reviewer (read-only, isolated context)."
  - Convergence rule: final-arbiter accepts/rejects findings; a single substitute round that resolves all ★ findings may be treated as convergence at the agent's judgment with user sign-off.
  - Lock phrase: `_Locked via grill-with-docs — by Claude + Eddie. Terms per CONTEXT.md._` at top of plan.

- [ ] **`docs/reviews/` vs `.agent/reviews/` naming convention** — Carry-over from Jun 21. Still needs a 2-line comment in `.agent/workflows/nightly-review.md` clarifying: `docs/reviews/` = grill-with-docs plan review logs (task-scoped); `.agent/reviews/` = nightly self-reflection reports (date-scoped).

## Permission Prompts to Reduce

- [ ] **`find ~/.claude/projects/ -name "*.jsonl"` and `ls ~/.claude/projects/`** — Carry-over from Jun 21. These exact commands run every nightly review. Candidates for `permissions.allow`: `Bash(find ~/.claude/projects/* -name "*.jsonl")`, `Bash(ls ~/.claude/projects/*)`.

- [ ] **`taskkill` and `netstat` (Windows-only)** — Carry-over from Jun 21. Only relevant on the user's Windows machine, not the remote container. Candidates if still triggering on Windows: `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

## Notes

- **PR #4 custom exercises is a solid, well-layered implementation.** Migration → query module → contracts → route → feature client → UI — each layer has focused tests. The substitute adversarial review resolved all 8 blocking findings before code was written.

- **Root `PLAN.md` / `PLAN-REVIEW-LOG.md` are the most immediately actionable cleanup items.** They'll create a confusing artifact collision the next time grill-with-docs runs. Moving them is a 2-minute follow-up commit.

- **Graphify is now 49 commits and one entire feature behind.** `graphify-out/wiki/index.md` has no entry for `CustomExercise`, `custom-exercise.ts`, `api.exercises.custom`, or `exercise-images`. Any agent told to "use the graph first" for exercise domain work will get actively wrong navigation.

- **Smoke gap for `/api/exercises/custom`** is straightforward to fix — add 4 entries (GET/POST/PATCH/DELETE all private, all should return 401 for unauthenticated).

- **`.playwright-mcp/` still not gitignored** — Carry-over from Jun 21. Not blocked by the current work, but will resurface if Playwright MCP is used again.

- **`.dev-server.log` / `.dev-server.err.log` not gitignored** — Carry-over from Jun 14 (now 8 days). `.gitignore` covers `server.log` and `server.err.log` but not the `.dev-server.*` variants that the dev-server workflow creates.

- **Open perf items (P6, P7, P8, P9, P12) are unchanged** — ilike/FTS search, full-catalog scan for suggested, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. Still scale-gated, not urgent.

- **Nightly review path mismatch needs a fix in the workflow prompt** — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` (Windows path) doesn't resolve in the remote Linux container. The `.agent/workflows/nightly-review.md` prompt should either: (a) compute the path dynamically (`ls ~/.claude/projects/` to find the actual project dir), or (b) add a note that in remote sessions no transcripts are available and the review falls back to git/code analysis. Either way, the current silent failure wastes the first few tool calls of every remote run.
