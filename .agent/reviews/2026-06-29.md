# Nightly Review — 2026-06-29

_Transcript scan note: **9th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container. Only 1 JSONL at `~/.claude/projects/-home-user-GymTracker/` — the current bootstrap session (27 lines). No memory directory accessible. Analysis based on git log, confirmed code delta (none — quiet day), and carry-over items from Jun 28 report._

**Positive change since Jun 28:** `HEAD` is no longer detached — `git symbolic-ref HEAD` returns `refs/heads/main`. The detached-HEAD risk documented in prior reports is resolved for this run. Rule addition still needed so future sessions know to verify.

_Activity since Jun 28: **No new code commits.** Only commit is the nightly review itself (`c9f2c1e chore(agent): nightly review 2026-06-28`). Repository on `main`, clean. All open items carry forward unchanged except the detached HEAD status._

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (unresolved — day counts from first report)

- [ ] **Object identity from adapter hooks must be memoized (ESCALATE — from c748069, production incident)** — `useAuth()` called `adaptUser()` on every render, building a fresh object each time. Consumers listing `user` in a dep array re-ran every render, driving an infinite edge-request loop (~280k Vercel function invocations/day, ~78% of Hobby cap). Fix: `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule: any hook that calls an adapter or constructor to transform context data must memoize its output on the stable source reference.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — When the full dataset is already in memory, `runSearch` can short-circuit the API call and serve from in-memory state. Shape: `if (filters.favourites) { setExercises(state.favoriteExercises); setTotal(n); setHasMore(false); return; }`. Only when: client has the full set and the filter is a view, not a new query.

- [ ] **`reflectFavouritesIfActive` — mutation-aware filter view sync (carry-over)** — Every mutation path (toggle success, refresh) must call a `reflectXIfActive(newSet)` helper to update the displayed list. Without this, the filter view goes stale after mutation.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}` on every control, `aria-busy={togglingId === thisId}` on the active one.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent UI wiring behind a mode check when a component is used in multiple contexts.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`. Mutate per-test inside `vi.mock`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — POST/DELETE returns `new Promise(() => {})` when `pendingMutation: true` is passed to the fetch stub.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`, so unauthenticated routes are also tracked.

- [ ] **`docs/handoffs/` directory referenced but not committed (ESCALATE — day 3)** — `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md` referenced in commit `c748069` does not exist. Commit the doc or drop the convention.

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (ESCALATE — 9 reports, P0)** — Add to §Read First: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately."

- [ ] **Detached HEAD resolved but rule still missing (ESCALATE — status improved)** — HEAD is on `main` today (resolved). Still add to §Git: "Remote/cloud sessions may start in detached HEAD. Verify with `git symbolic-ref HEAD` before committing; run `git checkout main` if detached."

- [ ] **`status.md` / `decisions.md` location (ESCALATE — 7 reports)** — These live in the Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Plan file placement convention — PLAN.md / PLAN-REVIEW-LOG.md still at root (ESCALATE — day 7)** — Both files remain at `/home/user/GymTracker/PLAN.md` and `/home/user/GymTracker/PLAN-REVIEW-LOG.md`. Correct location: `docs/plans/custom-exercises.md` and `docs/plans/custom-exercises-PLAN-REVIEW-LOG.md`. 2-minute commit.

- [ ] **Smoke test coverage rule for private routes (ESCALATE — 7 reports)** — `/api/exercises/custom` and `/api/exercise-favorites` have no `assertUnauthorized` smoke coverage. Add to §Testing: every new `privateMethod` route needs `assertUnauthorized` before merge.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` error codes from PR #8 undocumented. Add under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented. Add under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches. Add under §Architecture.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Do not call `supabase.rpc()` directly. Add under §Architecture.

- [ ] **`ConflictError` → 409 added to errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect` for one-time seeds.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`. Add under §API + Auth or Testing.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (ESCALATE — 6 reports, confirmed)** — `.gitignore` covers `server.log`/`server.err.log` but not `.dev-server.*`. Add both patterns. Confirmed today: only `server.log` and `server.err.log` appear in `.gitignore`.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Add to §Shell: "In remote sessions substitute Codex Act 2 with an isolated-context `Agent` tool call."

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Add rule: validate `file.size ≤ 5 MB` client-side before `uploadCustomExerciseImage`.

---

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (9th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in-repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge commits. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day. If memory tracks production incidents, add this.

- [ ] **New routes/API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE) added in PR #9. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow (ESCALATE — 9 consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase. Without it the process is rediscovered from scratch each time.

- [ ] **Fix nightly-review workflow prompt path (ESCALATE — 9 reports, P0)** — `.agent/workflows/nightly-review.md` (the `## Prompt` section) hardcodes `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` which has **never resolved** in 9 consecutive remote runs. Correct path: `~/.claude/projects/-home-user-GymTracker/`. One-line fix.

- [ ] **Adapter-hook identity audit skill (carry-over — from c748069)** — Grep for `useMemo`-less `adaptX(y)` or `new X(y.field)` calls in hook bodies to catch infinite-loop risks before they ship.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — `docs/plans/` = feature plans + grill-with-docs review logs. `.agent/reviews/` = nightly self-reflection. Update `.agent/workflows/nightly-review.md` with this distinction.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. The sync process and whether new skills must be mirrored are undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` exists in the project (ESCALATE — 9 reports)** — `permissions.allow` has never been configured. All Bash commands go through prompts. Create `.claude/settings.json` with at minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(wc -l ~/.claude/projects/*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** — Fire on every nightly review run. Highest-priority candidates.

- [ ] **`git symbolic-ref HEAD`** — New pattern observed today. Useful for start-of-session HEAD verification. Low-risk read-only op.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **No new code activity today.** Only commit since Jun 28 is the nightly review itself. All open items carry forward.

- **Detached HEAD resolved.** `git symbolic-ref HEAD` returns `refs/heads/main` — this is a first. All previous remote sessions started detached. Rule addition to §Git still warranted so future sessions verify before assuming.

- **CRITICAL — fix the nightly-review path (9th report, P0).** The single-line fix to `.agent/workflows/nightly-review.md` would eliminate wasted tool calls and the transcript note on every run. This is the highest-ROI change available.

- **PLAN.md / PLAN-REVIEW-LOG.md still at root — day 7.** Files confirmed at `/home/user/GymTracker/PLAN.md` and `/home/user/GymTracker/PLAN-REVIEW-LOG.md`. Correct home: `docs/plans/`. 2-minute commit.

- **`docs/handoffs/` still absent — day 3.** `c748069` commit message references a file that doesn't exist. Either create the directory and add the handoff doc, or remove the convention.

- **`.dev-server.log` not gitignored — confirmed.** `.gitignore` has `server.log` / `server.err.log` but not `.dev-server.log` / `.dev-server.err.log`. If the dev server writes these files, they'll be untracked noise.

- **`grill-with-docs` workflow still unwritten — 9 flags.** It is demonstrably established practice. The workflow doc must exist before the next feature plan phase starts.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` still have no `assertUnauthorized` smoke coverage.

- **Open perf items (P6–P9, P12)** — Vercel Analytics added in `a9e3dbc` is now live. Worth revisiting after a week of production traces.
