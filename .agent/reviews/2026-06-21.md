# Nightly Review — 2026-06-21

_Transcript scan note: The session path `C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote container (Linux env maps transcripts to `-home-user-GymTracker/`). Only 1 JSONL found there — the current session, which has no prior turns to analyze. Analysis below is based on code delta since the last review (Jun 14), git log, doc artifacts, and carry-over items from the Jun 14 review._

_Activity since Jun 14: 10 commits across 3 merged PRs (perf tier-1 × 3 batches, workout location feature, docs restructure, CLAUDE.md regen) + PR #5 (calendar TZ + prefill fix, merged Jun 21). ~95 source files touched. Most active sprint in 6 weeks._

---

## Proposed CLAUDE.md Additions

- [ ] **Document the contract vs. runtime validation split** — The calendar fix introduced a clear pattern: zod schemas in `src/lib/api/contracts/` are test-only; `makePrivateMethod` does NOT parse bodies. Every route handler that reads a `date` field now calls `isValidCalendarDate()` directly and returns `badRequest()` before querying. This is now established practice across 3 routes (`api.workout-sessions`, `api.calendar-data`, `api.workout-details`) but is not in CLAUDE.md. Add to the API + Auth section: "Contracts back tests only — `makePrivateMethod` does not enforce them at runtime. Route handlers must validate inputs explicitly (see `src/lib/api/errors.ts` helpers) before querying. See `src/lib/api/contracts/calendar-date.schema.ts` for the shared-schema pattern."

- [ ] **Document the `calendarDate` schema + `isValidCalendarDate` convention** — Any future route that touches `workout_sessions.date` needs to use `calendarDate` from `src/lib/api/contracts/calendar-date.schema.ts` (backed by `isValidCalendarDate` from `src/lib/utils/calendar.ts`). Add a one-liner to the API + Auth section or a new "Date handling" subsection: "For `YYYY-MM-DD` calendar dates use `calendarDate` schema. Never use `new Date('YYYY-MM-DD')` (UTC-midnight shift) or `toISOString().slice(0,10)` (UTC write). Use `getLocalCalendarDate()` to write and `parseCalendarDate()` to display."

- [ ] **`.playwright-mcp/` should be gitignored** — Screenshots accumulated in `.playwright-mcp/` during the calendar-theming work (Jan–May), were committed, then removed in the Jun 18 restructure commit. The directory is not gitignored. Next time Playwright MCP is used the screenshots will appear as untracked again. Add `.playwright-mcp/` to `.gitignore`.

- [ ] **Remote sessions run in detached HEAD** — For the second consecutive review, `git status --short --branch` shows `## HEAD (no branch)`. The remote container clones to a detached HEAD. Any `rtk git checkout <branch>` step from CLAUDE.md that assumes a named branch will fail silently or hit wrong assumptions. Add a note to the Git section: "Remote/cloud sessions (Claude Code on the web) start in detached HEAD. Before branching or committing, confirm with `git log --oneline -1` and run `git checkout -b <branch>` or `git checkout main` as needed."

- [ ] **Graphify trigger threshold now routinely exceeded — make it explicit** — PR #5 alone touched 25+ source files. The 3 perf PRs together touched 95. Graphify has not been run since before the Jun 3 sprint (now 18+ days). The "≥5 files" trigger in CLAUDE.md and the skill is too easily ignored. Consider: (a) lower the friction by noting `graphify update .` takes ~30s and always belongs at end of large sessions, or (b) elevate it to a required step in `gymmie-verification` (currently step 5 is conditional). At minimum, call out the current stale state.

## Memory Updates Needed

- [ ] **No `memory/` directory exists** — The path `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/memory/` (Windows) and `~/.claude/projects/-home-user-GymTracker/memory/` (Linux) both returned empty. If memory files exist on the user's Windows machine and are not synced to remote, a remote review cannot audit them. Flagging: confirm whether memory files need to exist in the remote container, or whether the nightly review prompt should skip the memory audit when running in a remote context.

- [ ] **Active branch is `main`** — Carry-over from Jun 14. All 5 PRs merged into `main`. Confirm no stale feature branches need pruning (`edlokedev/fix/calendar-tz-and-saved-workout-prefill`, `edlokedev/perf/launch-tier1-quick-wins` × 3 should be deletable post-merge).

- [ ] **Graphify is stale — ~18 days** — Graph has not been updated since before the Jun 3 sprint. Does not reflect: workout-templates, exercise discovery, set prefill, dashboard simplification, mobile collapse, performance layer (cache.ts, prefetch patterns), workout locations, calendar date utilities, or the full calendar TZ fix. CLAUDE.md says "Always use the graph before raw search" — that instruction is currently misleading because the graph is significantly outdated.

## New Skills / Workflows

- [ ] **`grill-with-docs` planning workflow** — Used twice in the past week (`perf-audit-2026-06-16.md` and `calendar-timezone-and-prefill-plan-review-2026-06-21.md`). Pattern: (1) write implementation plan, (2) submit to adversarial Codex review rounds (logged in `docs/reviews/<name>.md` with `MAX_ROUNDS`, `PLAN_FILE`, `LOG_FILE` header), (3) revise on each REVISE verdict, (4) lock on APPROVED. No `.agent/workflow/` exists for this yet. Create `.agent/workflows/grill-with-docs.md` documenting the header format, round logging convention, and the "lock — no code until APPROVED" rule. The Jun 21 review went 4 revise rounds + 1 approve (20 findings), demonstrating it catches real issues that were not in the original plan.

- [ ] **`docs/reviews/` vs `.agent/reviews/` naming distinction** — Two distinct review types now coexist but aren't distinguished in any doc: `docs/reviews/` holds plan-review logs (grill-with-docs output for a specific task) and audit write-ups; `.agent/reviews/` holds nightly self-reflection reports. Add a comment in `.agent/workflows/nightly-review.md` and the grill-with-docs workflow clarifying which directory each type goes in.

- [ ] **`.codex/skills/` mirror purpose undocumented** — Carry-over from Jun 14. `.codex/skills/gymmie-verification/SKILL.md` is identical to `.agent/skills/gymmie-verification/SKILL.md`. Origin and sync process are still undocumented. Add a note to the skill-creation workflow (or CLAUDE.md) explaining whether `.codex/` is for a second toolchain (e.g. Codex/OpenAI) and whether new skills need to go in both.

## Permission Prompts to Reduce

- [ ] **`taskkill` and `netstat` (Windows-only)** — Carry-over for 13+ days. These only apply on the user's Windows machine. The remote container is Linux; the nightly review can't verify they still prompt there. Still recommended to allowlist if they continue to appear in Windows sessions: `Bash(taskkill //F //IM node.exe*)`, `Bash(taskkill //F //PID *)`, `Bash(netstat -ano*)`.

- [ ] **`find ~/.claude/projects/ -name "*.jsonl"` and `ls ~/.claude/projects/`** — This nightly review itself triggered these read-only Bash commands. They will repeat every nightly run and are safe. Candidates for `permissions.allow` in `.claude/settings.json`: `Bash(find ~/.claude/projects/* -name "*.jsonl")`, `Bash(ls ~/.claude/projects/*)`.

## Notes

- **Biggest sprint since launch (Jun 14–21):** 5 PRs merged, 10 commits, ~95 files. Three carry-over items from Jun 14 were resolved: `gymmie-verification` is now referenced in CLAUDE.md Commands; review files (May 27–Jun 14) are now committed via the Jun 18 restructure commit; docs dirs were cleaned up (adr→adrs, audits→reviews, implementation-plans→plans).

- **Calendar TZ fix was deeper than it looked:** The `grill-with-docs` round found 20 issues across 4 revise rounds — the original plan missed the query-window UTC split, the summary-math UTC dependency, missing runtime validation, impossible-date validation, streak dedup, and partial-range bad requests. The adversarial review process clearly earned its keep here. The fix touched 25 source files and introduced 3 new shared utilities + a zod schema.

- **P6, P7, P8, P9, P12 still outstanding** from the perf audit: SQL ilike/FTS for search, suggested-exercises full-catalog scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. These are scale-gated and low-urgency now but should land before user growth.

- **Outstanding from Jun 14 review (still open):** graphify stale (now 18 days), `.dev-server.log` / `.dev-server.err.log` not gitignored (still not in `.gitignore`), `.codex/skills/` mirror undocumented, `taskkill`/`netstat` allowlist.

- **`test-auth-schema.js` / `test-user-session.js`** — These root-level files appear in the `git diff` stat for recent PRs but are not present in the working tree (deleted or never committed). If they were one-off debugging scripts, confirm they are fully removed and won't reappear.
