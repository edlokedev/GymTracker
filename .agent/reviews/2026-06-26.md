# Nightly Review — 2026-06-26

_Transcript scan note: **6th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container. Only 1 JSONL found at `~/.claude/projects/-home-user-GymTracker/` — the current bootstrap session (28 lines, no prior turns to analyse). Analysis based on git log since Jun 24, code delta from PR #8 (`feature/edit-exercise`), and carry-over items from Jun 24 report._

_Activity since Jun 24: **PR #8 merged** (`feat: change exercise on a logged workout`). One significant feature shipped — 523 lines across 15 files. Commit `2cbbd95` by ed dev; merge `334d8b5` by edlokedev. No other code commits._

---

## Proposed CLAUDE.md Additions

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (ESCALATE — 6 reports, P0)** — `graphify-out/` is gitignored and never present in remote/cloud sessions. Step 5 in §Read First says "Always use the graph before raw search" which wastes tool calls in every remote session. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately. The graph rule applies only to local desktop sessions where `graphify update .` has been run."

- [ ] **Detached HEAD in remote — must checkout before committing (ESCALATE — 5 reports)** — Every remote session starts with HEAD detached (confirmed again this run: `## HEAD (no branch)`). The §Git section should say: "Remote/cloud sessions start in detached HEAD. Run `git checkout main` (or `git checkout -b <branch>`) before branching or committing."

- [ ] **`status.md` / `decisions.md` location (ESCALATE — 4 reports)** — Step 4 in §Read First lists these without clarifying they live in the Odin workspace root (parent of `GymTracker/`), not in this repo. Add: "These live in the Odin workspace root, not in `GymTracker/`. Skip when running in remote sessions."

- [ ] **Plan file placement convention (ESCALATE — 4 reports, `PLAN.md` still at root after 4 days)** — `PLAN.md` and `PLAN-REVIEW-LOG.md` remain at the repo root. Add to Architecture/Docs: "Feature plans → `docs/plans/<feature>.md`. Grill-with-docs logs → `docs/reviews/<feature>-review-YYYY-MM-DD.md`. Never use root-level `PLAN.md` / `PLAN-REVIEW-LOG.md`."

- [ ] **Smoke test coverage rule for `privateMethod` routes (ESCALATE — 4 reports, now includes PATCH /api/workout-sets)** — `/api/exercises/custom` (GET/POST/PATCH/DELETE) and the new `PATCH /api/workout-sets` (repoint) are both absent from `scripts/smoke.ts`. Add to §Testing: "Every new `privateMethod` API route must have a `{ expectStatus: 200 }` + `assertUnauthorized` entry in `scripts/smoke.ts` before the PR merges."

- [ ] **Custom SQLSTATE namespacing convention (NEW — from PR #8)** — `repoint_workout_exercise` introduces `GYM01`–`GYM04` custom SQLSTATEs. The pattern is: define states in the migration comment, map them exhaustively in the query-layer `switch`, and throw typed errors from `src/lib/api/errors.ts`. Undocumented. Add under §Architecture: "Custom Postgres SQLSTATEs use the `GYM` namespace (e.g. `GYM01`). Each SQLSTATE is: (a) declared in the migration with failure semantics, (b) mapped in the query-layer `switch` to a typed error from `errors.ts`, (c) translated to an HTTP status by the route layer. The `default:` branch always calls `assertPostgresOk(error)`."

- [ ] **`SECURITY INVOKER` RPC + row-lock pattern (NEW — from PR #8)** — `repoint_workout_exercise` is the first SECURITY INVOKER function. The comment in the migration explains why (RLS stays active). This is now an established pattern for multi-row atomic mutations that can't be expressed as a single PostgREST UPDATE. Add under §Architecture: "Multi-row atomic mutations use `SECURITY INVOKER` RPCs (not DEFINER) so RLS stays active under the caller's JWT. Take a `FOR UPDATE` row-lock on the parent session row to serialize concurrent operations. Signal failure modes via custom SQLSTATEs (`GYM` namespace)."

- [ ] **Optimistic local fallback pattern for unsaved sessions (NEW — from PR #8)** — `changeExercise` in `useWorkoutSession.ts` branches: if `session?.id` is set, call the API + reload; otherwise mutate local state directly (`setExercises(...replaceExerciseInWorkout(...))`). This same unsaved/saved branch is used in other commands. Add under §Architecture: "State mutations in `useWorkoutSession` branch on `session?.id`: persisted sessions → API call then `loadSessionData`; unsaved sessions → local state update only. New commands must handle both branches."

- [ ] **`queryClient.rpc<T>()` typed wrapper (NEW — from PR #8)** — `query-client.ts` now exposes a typed `rpc<TData>()` method alongside the existing query builders. Previously RPC calls bypassed the client. Add a note to §Architecture or the query pattern: "Use `queryClient(supabase).rpc<ReturnType>('fn_name', args)` for all Postgres function calls — do not call `supabase.rpc()` directly."

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23/24)** — Confirmed in 3 locations. Add one-liner under §Architecture: "One-time seed effects use a ref guard (`useRef<SomeId | null>(null)`) — check and set inside `useEffect` to run exactly once per entity load without putting the entity in the dep array."

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23/24)** — Add: "When passing an async model function to a `() => void` prop, discard the return at the call site: `onFoo={() => { void modelFn() }}`."

- [ ] **`.playwright-mcp/` not gitignored (ESCALATE — 4 reports)** — Will reappear as untracked on the next Playwright MCP run. Add `.playwright-mcp/` to `.gitignore`.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (ESCALATE — 4 reports)** — `.gitignore` covers `server.log`/`server.err.log` but not the `.dev-server.*` variants. Add both glob patterns.

- [ ] **`ConflictError` → 409 added to errors.ts (NEW — from PR #8)** — `ConflictError` is now a typed error in `src/lib/api/errors.ts` alongside `NotFoundError`, `ForbiddenError`, `BadRequestError`. CLAUDE.md §API + Auth doesn't list these. Add: "Typed errors for route→HTTP translation live in `src/lib/api/errors.ts`: `NotFoundError` (404), `ForbiddenError` (403), `BadRequestError` (400), `ConflictError` (409). Route handlers catch these; all others become 500."

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21/22/23/24)** — `codex: command not found` for every remote feature run. Add to §Shell: "In remote sessions substitute Codex Act 2 with an isolated-context `Agent` tool call."

- [ ] **Image upload file size validation missing (carry-over Jun 22/23/24)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Add: "`exercise-images` uploads must client-side-reject files ≤5 MB. Validate `file.size` before calling `uploadCustomExerciseImage`."

---

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (6th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in the repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Graphify stale — 80+ commits since last update** — The graph misses PR #4 (custom exercises), PR #5 (calendar TZ), PR #6 (completion bar), PR #7 (app icon), PR #8 (change exercise), plus all perf work. Any memory entry marking the graph as current is stale.

- [ ] **New migration namespace: `GYM` SQLSTATEs** — If memory tracks error taxonomy or DB conventions, add: "Custom SQLSTATEs use the `GYM` namespace; see `20260622000000_repoint_workout_exercise.sql` for first usage and `src/lib/api/errors.ts` for the full typed-error catalogue."

---

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow (ESCALATE — 6 consecutive reports, P0)** — Adversarial plan review has run on at least 3 shipped features without a formal workflow. Create `.agent/workflows/grill-with-docs.md` covering: header params (`MAX_ROUNDS`, `PLAN_FILE`, `LOG_FILE`), REVISE/APPROVED loop, convergence rule, lock phrase, and remote-fallback (Agent tool in `isolation: 'worktree'` instead of Codex CLI).

- [ ] **Fix nightly-review workflow prompt path (ESCALATE — 6 reports, P0)** — `.agent/workflows/nightly-review.md` still hardcodes `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` which never resolves in a remote container. Replace with: "Run `ls ~/.claude/projects/` first and use the found directory name." This single fix saves 4–6 wasted tool calls at the start of every run.

- [ ] **`docs/reviews/` vs `.agent/reviews/` naming (carry-over)** — Add to `.agent/workflows/nightly-review.md`: "`docs/reviews/` = grill-with-docs plan-review logs (task-scoped). `.agent/reviews/` = nightly self-reflection reports (date-scoped, automated)."

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` is a copy of `.agent/skills/gymmie-verification/`. The sync process (if any) and whether new skills must be mirrored are undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` exists in the project** — `permissions.allow` has never been configured. All Bash commands go through prompts. Create `.claude/settings.json` with at minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** (6th consecutive report) — Fire on every nightly review run. Highest-priority permission candidates.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — Candidates: `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **CRITICAL — fix the nightly-review workflow prompt (6th report, P0).** The hardcoded Windows path `C--Users-Hii-Documents-Code-GymTracker` has never resolved in any remote run. 6 consecutive sessions have each wasted 4–6 tool calls rediscovering the correct path. This is the single highest-impact change available. Fix: replace hardcoded path in `.agent/workflows/nightly-review.md` with a dynamic `ls ~/.claude/projects/` step.

- **PR #8 merged — new patterns now established.** The change-exercise feature introduced three undocumented but reusable patterns: SECURITY INVOKER RPCs with row-locking, GYM-namespaced SQLSTATEs, and the saved/unsaved session branch in hook commands. All three are likely to recur in future features; document them before the next feature starts.

- **`PATCH /api/workout-sets` (repoint) has no smoke coverage.** This is now the second private route without `assertUnauthorized` smoke coverage (after `/api/exercises/custom`). The smoke test gap is a real regression risk — a RLS policy change would pass CI silently.

- **`PLAN.md` / `PLAN-REVIEW-LOG.md` cleanup is trivial and 4 days overdue.** Moving them is a 2-minute commit with zero code risk. `docs/plans/custom-exercises.md` and `docs/reviews/custom-exercises-review-YYYY-MM-DD.md` are the right destinations.

- **`grill-with-docs` workflow has been flagged for 6 consecutive reviews.** It has now been used for at least 3 shipped features and is de facto established practice. Creating the workflow doc before the next feature is critical — without it each agent session rediscovers the process from scratch.

- **Open perf items (P6–P9, P12) unchanged** — SQL ilike/FTS, suggested-exercises full-catalog scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. Scale-gated, low urgency.
