# Nightly Review — 2026-07-12

_Transcript scan note: Remote container holds only 1 JSONL (`/root/.claude/projects/-home-user-GymTracker/` — current session only). Historical transcripts from the Windows machine remain unavailable. Analysis based on current-session trace, git log, and repo-state inspection. Carry-overs from Jul 11 report preserved and updated._

**HEAD status:** DETACHED at session start (22nd consecutive session). Reattached via `git fetch origin main && git checkout main && git pull origin main`. Fast-forwarded 4 nightly-review commits (Jul 8–11).

**Cron path mismatch still active — 22nd session:** Nightly run tried `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` (old Windows path embedded in cron trigger) before discovering `/root/.claude/projects/-home-user-GymTracker/`. This wasted 3 tool calls. Workflow file on `main` has had the correct Linux path since `7806280`. Live cron cannot be corrected from a remote session.

**Activity since Jul 11:** No code commits. Only nightly review commits. No open PRs. All Jul 11 carry-overs remain unresolved.

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (count = consecutive sessions flagged)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (22 reports, P0)** — §Read First "always use the graph" is unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (22/22 reports, P0)** — Add to §Git: "Remote/cloud sessions almost always start in detached HEAD. Run `git symbolic-ref HEAD 2>/dev/null || echo DETACHED`; if detached, `git fetch origin main && git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (11 reports, P0)** — Without the pull, local main lags remote by all commits since the last cloud session.

- [ ] **No `.claude/settings.json` exists (22 reports, P0)** — `.claude/` is gitignored. File must be created per-machine/container. No permission allowlist is active; every shell command requires manual approval in interactive sessions.

- [ ] **`queryKeys` centralized factory — add to §Architecture (carry-over from Jul 5)** — `src/lib/api/query-keys.ts` exports the only valid cache-key source per ADR-0007. Never construct TanStack Query keys as inline arrays at call sites.

- [ ] **`test/queryWrapper.tsx` mandatory for hook/component tests touching TanStack Query (carry-over from Jul 5)** — `createTestQueryClient()` + fresh `QueryClientProvider` per test; omitting it causes cache bleed between tests.

- [ ] **Feature hook decomposition threshold (carry-over from Jul 5)** — When a feature hook exceeds ~200 lines or manages 3+ independent concerns, split into focused sub-hooks (pattern from `useExerciseLibrary` split in PR #10).

- [ ] **Auth guard inconsistency — two remaining active treatments, do not add a third (22 reports)** — Two patterns: skeleton+LoginPage vs no guard at all. Converge on `LoginPage` render pattern until a shared `RequireAuth` guard is built.

- [ ] **Side effect inside React state updater is a bug — banned pattern (carry-over from Jul 5)** — `useWorkoutSession.ts:518-549`, `useCalendarData.ts:96-113`, `WorkoutSessionManager.tsx:202-292`. Never put network calls, `setState`, or DOM ops inside a `setState` updater function.

- [ ] **Transactionless delete-all-then-insert banned for multi-row writes (carry-over from Jul 5)** — `workout-templates.ts:284-305` can permanently empty a template on mid-write failure. Multi-row mutations must use a SQL RPC or transaction.

- [ ] **URL-rewrite migration pattern (carry-over from Jul 5)** — Dedicated migration file with `UPDATE ... SET url = REPLACE(url, old_prefix, new_prefix)`. Add to §Data.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `src/lib/auth/index.ts:62` correctly uses `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule missing from CLAUDE.md.

- [ ] **`docs/handoffs/` absent — day 15** — `c748069` references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. `docs/reviews/` is the more sensible home.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 20** — Both still at repo root. Correct location: `docs/plans/`. `git mv` needed.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (18 reports)** — `.gitignore` has `server.log` (line 48) and `server.err.log` (line 49) but not `.dev-server.*`. One-line fix; lowest-effort open item.

- [ ] **`status.md` / `decisions.md` in Odin workspace, not repo (20 reports)** — Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage for private routes (20 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` confirmed absent from `scripts/smoke.ts`. Only 6 paths covered with `assertUnauthorized`; these 3 are not among them.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Substitute with isolated `Agent` tool call.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }`

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag is set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — Validate `file.size ≤ 5 MB` in `CustomExerciseForm.tsx`.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check.

- [ ] **`!data.set_order` rejects set_order 0 bug (open, unaddressed)** — `src/routes/api.workout-sets.ts:41` — `!data.set_order` rejects valid `set_order: 0`. Fix: `data.set_order == null`. Confirmed still present today.

- [ ] **`parsePaginationParams` shared helper missing (open, unaddressed)** — Pagination parsing is inconsistent across route handlers.

- [ ] **Body validation: adopt zod pattern for all mutating routes (open, unaddressed)** — `api.exercises.custom.ts` uses zod; other mutating routes blind-cast `await request.json()`.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 22nd consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in remote. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions.

- [ ] **Graphify stale — 100+ commits since last update** — PRs #4–#11 unrepresented. Graph unreliable for current codebase shape.

- [ ] **API surface stale** — `src/lib/api/query-keys.ts` and all TanStack Query hooks are new or migrated.

- [ ] **Production incident log** — useAuth memoization bug (c748069) consumed ~78% of Vercel Hobby 1M edge-request cap.

- [ ] **`docs/reviews/` now exists** — `docs/handoffs/` pattern may be superseded.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 22nd report)** — Live cron still fires with old Windows path `C--Users-Hii-Documents-Code-GymTracker`. This session wasted 3 tool calls searching the wrong path before finding `/root/.claude/projects/-home-user-GymTracker/`. CronDelete + CronCreate takes ~30 seconds and would eliminate this every run.

- [ ] **Nightly-review: hard-code JSONL path, stop searching (P1, new observation)** — The nightly prompt should specify the exact JSONL directory (`/root/.claude/projects/-home-user-GymTracker/`) rather than searching. Every session wastes 2–3 `find` calls discovering the same path.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (P1, 9th report)** — `python3 -c` stdin piping is fragile. Reliable extraction: `grep -o '"command":"[^"]*"'` for Bash inputs.

- [ ] **`gymmie-verification` skill: add detached HEAD pre-check (defensive, 22nd report)** — Step 0: `git symbolic-ref HEAD 2>/dev/null || echo DETACHED`; if detached, `git checkout main && git pull origin main`.

- [ ] **`grill-with-docs` workflow (20+ consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md`.

- [ ] **`require-auth-guard` refactor workflow** — 2 inconsistent auth guard patterns remain.

- [ ] **`query-key-audit` skill** — Verify no inline cache-key arrays remain: `grep -rn "useQuery\(\[" src/`.

- [ ] **`adapter-hook identity audit` skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror sync (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 22nd report** — `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find /root/.claude/projects/-home-user-GymTracker/ -name \"*.jsonl\")",
        "Bash(ls /root/.claude/projects/-home-user-GymTracker/)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git fetch origin main)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l /root/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(grep -n*)",
        "Bash(grep -c*)",
        "Bash(python3 -c*)",
        "Bash(cat /home/user/GymTracker/.agent/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)",
        "Bash(ls /home/user/GymTracker/src/routes/)",
        "Bash(ls /home/user/GymTracker/scripts/)"
      ]
    }
  }
  ```

- [ ] **`find /root/.claude/projects/`** — fires on every nightly run; 22nd consecutive.
- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.
- [ ] **`git fetch origin main` / `git checkout main` / `git pull origin main`** — mandatory session-start sequence.
- [ ] **`grep -o '...' *.jsonl`** — JSONL content extraction.
- [ ] **`python3 -c` JSONL parse** — transcript analysis.

---

## Notes

- **Cron path mismatch is the single highest-ROI fix — 22nd consecutive session.** This session wasted 3 tool calls (`ls ~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/`, `find ~/.claude/projects/ -name "*.jsonl"`, `find ~/.claude/ -name "*.jsonl"`) before locating `/root/.claude/projects/-home-user-GymTracker/`. CronDelete + CronCreate (correct path) takes ~30 seconds.

- **Hard-code JSONL path in the nightly prompt (new).** The discovery pattern is now stable over 22 sessions: the JSONL always lives at `/root/.claude/projects/-home-user-GymTracker/<uuid>.jsonl`. The prompt should say this explicitly instead of requiring a search every time.

- **HEAD detached (22/22 sessions).** Remote containers clone at a commit hash, not a named branch ref. The fix is simple and well-understood; the CLAUDE.md rule is simply missing after 22 reports.

- **Repo quiet.** No code commits since PR #11 merge (`dc8cafa`). No open PRs.

- **`!data.set_order` bug still open — confirmed.** `src/routes/api.workout-sets.ts:41`. One-line fix: `data.set_order == null`.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 20.** `docs/plans/` is active. `git mv` is the fix.

- **`.dev-server.log` not gitignored — 18th report.** Lowest-effort open item. One line to `.gitignore`.

- **`docs/handoffs/` absent — day 15.** `docs/reviews/` is the better home.

- **Smoke coverage gaps confirmed.** `/api/exercises/custom`, `/api/exercise-favorites` are not in `scripts/smoke.ts`. `PATCH /api/workout-sets` has GET coverage but no PATCH test.

- **`graphify update .` overdue.** PRs #4–#11 unrepresented. Graph-based navigation unreliable.
