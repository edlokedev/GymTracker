# Nightly Review — 2026-06-23

_Transcript scan note: **4th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not resolve in the remote Linux container. Only 1 JSONL is present (`-home-user-GymTracker/`) and it contains only the current session (no prior turns). All analysis below is based on the code delta since the Jun 22 review, git log (commits `f876b63`, `8d389e1`, `35f3768`, `b08499f`), and carry-over items. The workflow prompt must be fixed — see Notes._

_Activity since Jun 22: 2 merged PRs — PR #6 (exercise completion bar seed + type fix, 3 source files, ~88 lines) and PR #7 (app icon branding update, 9 assets). Low-volume day; no new features._

---

## Proposed CLAUDE.md Additions

- [ ] **`graphify-out/` is gitignored and absent in remote containers — remote sessions can NEVER use the graph** — CLAUDE.md §Read First step 5 says "Always use the graph before raw search." `graphify-out/` is gitignored (confirmed), meaning remote/cloud sessions start with no graph at all. Any agent following step 5 will find no `graphify-out/wiki/index.md` and should fall through to raw search immediately. Add a caveat: "In remote sessions (Claude Code on the web) `graphify-out/` is never present — skip to `rg` search directly. The graph rule applies only to local desktop sessions where `graphify update .` has been run."

- [ ] **Detached HEAD in remote (3rd consecutive report, still absent from CLAUDE.md)** — Every remote nightly session shows `HEAD detached from refs/heads/main`. Add to the Git section: "Remote/cloud sessions start in detached HEAD. Before branching or committing, run `git checkout main` or `git checkout -b <branch>` first."

- [ ] **Ref-guarded one-time seed pattern** — Used in at least 3 places (`seededCompletionSessionIdRef` in `WorkoutSessionManager.tsx:105`, `startedTemplateIdRef` in `useWorkoutSession.ts:97`, `lastAppliedRouteKey` in `useExerciseLibrary.ts:88`). The pattern: `const fooRef = useRef<SomeId | null>(null)` guards an idempotent `useEffect` from re-running when its dependencies change after the first execution. Not documented. Add a one-liner in the Architecture or Styling section: "One-time seed effects use a ref guard (`useRef<SomeId | null>(null)`) to run exactly once per entity load without adding the entity to the effect dependency array."

- [ ] **`status.md` / `decisions.md` live in the Odin workspace, not this repo (carry-over Jun 22)** — CLAUDE.md step 4 lists them as "project state" without location. Add: "These live in the Odin workspace root (parent of `GymTracker/`), not in this repo. Skip when running in remote sessions."

- [ ] **Plan file placement convention (carry-over Jun 22)** — `PLAN.md` and `PLAN-REVIEW-LOG.md` remain at repo root 2 days after the Jun 22 review flagged them. Add to Architecture/Docs: "Feature plans → `docs/plans/<feature>.md`. Grill-with-docs review logs → `docs/reviews/<feature>-review-YYYY-MM-DD.md`. Never use root-level `PLAN.md` / `PLAN-REVIEW-LOG.md`."

- [ ] **Smoke test coverage rule for new `privateMethod` routes (carry-over Jun 22)** — `/api/exercises/custom` (GET/POST/PATCH/DELETE) still has no smoke entries. Add to the Testing section: "Every new `privateMethod` route must have a `{ expectStatus: 200 }` + `assertUnauthorized` entry in `scripts/smoke.ts`."

- [ ] **Async handler return-type wrapping gotcha** — PR #6 included `8d389e1`: `duplicateSession` returns `WorkoutSession | null` but the `onDuplicateWorkout` prop expects `() => void | Promise<void>`. Fix was to discard the return value at the call site. Add a one-liner under API + Auth or Testing: "When wiring an async model fn to a `() => void` prop, wrap it: `onFoo={() => { void modelFn() }}` — do not pass the fn reference directly if its return type doesn't match."

- [ ] **`.playwright-mcp/` not gitignored (carry-over Jun 21/22)** — Screenshots will reappear as untracked next time Playwright MCP is used. Add to `.gitignore`: `.playwright-mcp/`.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (carry-over multiple reviews)** — `.gitignore` covers `server.log` / `server.err.log` but the dev-server workflow creates `.dev-server.*` variants. Add both glob patterns.

- [ ] **Codex CLI unavailable in remote — remote fallback for grill-with-docs Act 2 (carry-over Jun 22)** — `codex: command not found` for 3 features in a row. Add to Shell section or grill-with-docs workflow: "In remote sessions, substitute the Codex adversarial review with an `Agent` tool call using `isolation: 'worktree'`. Record the pass as 'Substitute reviewer (isolated context)' in the review log."

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (4th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in the remote container. Memory audit cannot run here. Confirm whether memory files should be committed to the repo (e.g. `.agent/memory/`) or if the nightly review prompt should skip the memory audit in remote contexts.

- [ ] **Exercise completion bar bug is fixed (PR #6 merged Jun 22)** — If a memory entry tracked this as an open bug, mark it resolved.

- [ ] **App icon updated to `gymmie-icon.png` (PR #7 merged Jun 23)** — `manifest.json` now references `gymmie-icon.png` (512×512) as the primary icon. `logo192.png` and `logo512.png` are identical to `gymmie-icon.png` post-update. If branding state is tracked, update.

- [ ] **Graphify stale — 69 total commits, graph absent from remote** — If a memory entry says the graph is current or useful, flag as stale/inaccessible for all remote sessions.

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow missing (4th consecutive report — escalate)** — The adversarial plan review process has now run for 3 features (perf-audit, calendar-TZ, custom-exercises). Create `.agent/workflows/grill-with-docs.md` with: header format (`MAX_ROUNDS`, `PLAN_FILE`, `LOG_FILE`), REVISE/APPROVED loop, convergence rule, lock phrase, and remote fallback (Agent tool instead of Codex CLI).

- [ ] **`docs/reviews/` vs `.agent/reviews/` naming convention (carry-over Jun 21/22)** — Two distinct types coexist with no doc distinguishing them. Add a comment to `.agent/workflows/nightly-review.md` (and future grill-with-docs.md): "`docs/reviews/` = grill-with-docs plan-review logs (task-scoped, multi-round). `.agent/reviews/` = nightly self-reflection reports (date-scoped, automated)."

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14 onwards)** — `.codex/skills/gymmie-verification/` is identical to `.agent/skills/gymmie-verification/`. The sync process and purpose are still undocumented. Add a note explaining whether `.codex/` serves a second toolchain (e.g. Codex/OpenAI integration) and whether new skills must be mirrored.

## Permission Prompts to Reduce

- [ ] **`find ~/.claude/projects/ -name "*.jsonl"` and `ls ~/.claude/projects/`** (4th consecutive report) — These fire on every nightly review run and are safe read-only operations. Add to `permissions.allow` in `.claude/settings.json`: `"Bash(find ~/.claude/projects/* -name \"*.jsonl\")"`, `"Bash(ls ~/.claude/projects/*)"`.

- [ ] **`taskkill` and `netstat` (Windows-only, carry-over)** — Not testable from the remote container, but likely still prompting on the user's Windows machine. Candidates: `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

## Notes

- **Most urgent action: fix the nightly-review workflow prompt.** The `C--Users-Hii-Documents-Code-GymTracker` path is the Windows encoding of the project path and has never resolved in any remote run (4 runs). The prompt in `.agent/workflows/nightly-review.md` should be updated to either: (a) derive the path dynamically (`ls ~/.claude/projects/` first, use whatever project dir is found), or (b) explicitly document that transcript history is unavailable in remote sessions and the review should fall back to `git log` and code delta analysis. The current silent failure wastes 4–6 tool calls at the start of every run.

- **`graphify-out/` being gitignored is a fundamental conflict with the CLAUDE.md "use graph first" rule.** In remote sessions, the graph is never present. Agents following that instruction will waste time looking for a non-existent file. The CLAUDE.md rule needs a remote-context exception, or the graph should be committed (but the directory is explicitly gitignored, suggesting that was an intentional decision). Worth a 30-second clarification from the user.

- **PR #6 completion bar fix is a good reference for the ref-guard pattern.** The `seededCompletionSessionIdRef` approach (guard a `useEffect` by checking a ref before the effect runs, then set the ref after) avoids adding mutable state to the dep array and prevents re-seeding when the user edits a completed exercise. This pattern is also present in `useWorkoutSession.ts` and `useExerciseLibrary.ts`, making it established practice worth documenting.

- **Root `PLAN.md` and `PLAN-REVIEW-LOG.md` are now 2 days past the cleanup flag.** They are the custom-exercises planning artifacts from PR #4. Moving them to `docs/plans/custom-exercises.md` and `docs/reviews/custom-exercise-plan-review-2026-06-22.md` is a 2-minute follow-up commit with no code risk.

- **Smoke test gap for `/api/exercises/custom` remains unfixed.** GET/POST/PATCH/DELETE routes — all `privateMethod` — have no `assertUnauthorized` smoke entries. The smoke script does not know these routes exist.

- **Open perf items (P6, P7, P8, P9, P12) unchanged** — SQL ilike/FTS, suggested-exercises full-catalog scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. Still scale-gated, low urgency.

- **App icon change (PR #7) introduced redundancy in `manifest.json`** — `logo512.png` and `gymmie-icon.png` now point to the same image bytes (both 271 KB). Cosmetic, not a bug, but if PWA icon sizes matter, `logo192.png` (40 KB) is distinct enough to stay.
