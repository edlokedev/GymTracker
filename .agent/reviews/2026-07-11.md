# Nightly Review — 2026-07-11

_Transcript scan note: Remote container holds only 1 JSONL (`/root/.claude/projects/-home-user-GymTracker/` — current session only). Historical transcripts from the Windows machine remain unavailable in the cloud environment. Analysis based on current-session trace, git log, repo-state inspection, and carry-over from Jul 10 report._

**HEAD status:** DETACHED at session start (20/21 sessions now). Reattached via `git checkout main && git pull origin main`. Fast-forwarded to include Jul 8–10 review commits (`aa47067`–`58d4505`). The "leaving 3 commits behind" warning is the detached-HEAD ref becoming unreachable, not a lost-commit problem.

**Cron path mismatch still active — 21st session:** Live cron trigger used old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. Workflow file on `main` (`.agent/workflows/nightly-review.md`) has had the correct Linux path `-home-user-GymTracker/` since `7806280`. CronList is session-local; live cron cannot be inspected or corrected from here.

**Activity since Jul 10:** No code commits. Jul 10 nightly review (`58d4505`) was the only push. No open PRs. All Jul 10 carry-overs remain unresolved.

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (count = consecutive sessions flagged)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (21 reports, P0)** — §Read First "always use the graph" is unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (20/21 reports, P0)** — Add to §Git: "Remote/cloud sessions almost always start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (10 reports, P0)** — Without the pull, local main lags remote by all commits since the last cloud session.

- [ ] **No `.claude/settings.json` exists (21 reports, P0)** — `.claude/` is gitignored. File must be created per-machine/container. No permission allowlist is active; every shell command requires manual approval in interactive sessions.

- [ ] **`queryKeys` centralized factory — add to §Architecture (carry-over from Jul 5)** — `src/lib/api/query-keys.ts` exports the only valid cache-key source per ADR-0007. Never construct TanStack Query keys as inline arrays at call sites.

- [ ] **`test/queryWrapper.tsx` mandatory for hook/component tests touching TanStack Query (carry-over from Jul 5)** — `createTestQueryClient()` + fresh `QueryClientProvider` per test; omitting it causes cache bleed between tests.

- [ ] **Feature hook decomposition threshold (carry-over from Jul 5)** — When a feature hook exceeds ~200 lines or manages 3+ independent concerns, split into focused sub-hooks (pattern from `useExerciseLibrary` split in PR #10).

- [ ] **Auth guard inconsistency — two remaining active treatments, do not add a third (21 reports)** — Two remaining patterns: skeleton+LoginPage vs no guard at all. Converge on `LoginPage` render pattern until a shared `RequireAuth` guard is built.

- [ ] **Side effect inside React state updater is a bug — banned pattern (carry-over from Jul 5)** — `useWorkoutSession.ts:518-549`, `useCalendarData.ts:96-113`, `WorkoutSessionManager.tsx:202-292`. Never put network calls, `setState`, or DOM ops inside a `setState` updater function.

- [ ] **Transactionless delete-all-then-insert banned for multi-row writes (carry-over from Jul 5)** — `workout-templates.ts:284-305` can permanently empty a template on mid-write failure. Multi-row mutations must use a SQL RPC or transaction.

- [ ] **URL-rewrite migration pattern (carry-over from Jul 5)** — Dedicated migration file with `UPDATE ... SET url = REPLACE(url, old_prefix, new_prefix)`. Add to §Data.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `src/lib/auth/index.ts:62` correctly uses `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule still missing from CLAUDE.md to prevent regression.

- [ ] **`docs/handoffs/` absent — day 14** — `c748069` references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. `docs/reviews/` is the more sensible home.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 19** — Both still at repo root (`PLAN.md`, `PLAN-REVIEW-LOG.md`). Correct location: `docs/plans/` (which already exists and contains other plan files). `git mv` needed.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (17 reports)** — `.gitignore` has `server.log` (line 48) and `server.err.log` (line 49) but not `.dev-server.*`. One-line fix; lowest-effort open item.

- [ ] **`status.md` / `decisions.md` in Odin workspace, not repo (19 reports)** — Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage for private routes (19 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Substitute with isolated `Agent` tool call.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }`

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag is set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — Validate `file.size ≤ 5 MB` in `CustomExerciseForm.tsx`.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check.

- [ ] **`!data.set_order` rejects set_order 0 bug (open, unaddressed)** — `src/routes/api.workout-sets.ts:41` uses `!data.set_order` which rejects valid `set_order: 0`. Fix: `data.set_order == null`. Confirmed still present today.

- [ ] **`parsePaginationParams` shared helper missing (open, unaddressed)** — Pagination parsing is inconsistent. `api.exercises.recent.ts:6-11` clamps 1–50; other route handlers accept NaN/unbounded values.

- [ ] **Body validation: adopt zod pattern for all mutating routes (open, unaddressed)** — `api.exercises.custom.ts` uses zod; other mutating routes blind-cast `await request.json()`. Apply uniformly.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 21st consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions.

- [ ] **Graphify stale — 100+ commits since last update** — PRs #4–#11 all unrepresented. Graph is unreliable for current codebase shape. `graphify update .` needs to run from a machine with the package available.

- [ ] **API surface stale** — `src/lib/api/query-keys.ts` and all TanStack Query hooks are new or migrated; any memory entry tracking the API surface is stale.

- [ ] **Production incident log** — useAuth memoization bug (c748069) consumed ~78% of Vercel Hobby 1M edge-request cap. Add if memory tracks production incidents.

- [ ] **`docs/reviews/` now exists** — `docs/handoffs/` pattern may be superseded. Update any memory entry about review/handoff conventions.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 21st report)** — Live cron still fires with old Windows path `C--Users-Hii-Documents-Code-GymTracker`. Workflow file on `main` has the correct Linux path. Fix: CronDelete old job, CronCreate from `.agent/workflows/nightly-review.md`. Costs 2–3 wasted tool calls per session until fixed.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (P1, 8th report)** — `python3 -c` stdin piping is fragile in the remote environment. Reliable extraction: `grep -o '"command":"[^"]*"'` for Bash inputs, `grep -o '"name":"[^"]*"'` for tool names.

- [ ] **`gymmie-verification` skill: add detached HEAD pre-check (defensive, 21st report)** — Step 0: `git symbolic-ref HEAD 2>/dev/null || echo DETACHED`; if detached, `git checkout main && git pull origin main` before proceeding.

- [ ] **`grill-with-docs` workflow (20+ consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md`.

- [ ] **`require-auth-guard` refactor workflow** — 2 inconsistent auth guard patterns remain. A bounded workflow to converge remaining private routes to a shared `RequireAuth` guard.

- [ ] **`query-key-audit` skill** — After TanStack Query migration, verify no inline cache-key arrays remain at call sites: `grep -rn "useQuery\(\[" src/`. (Spot-check today found 0 violations — good, but formalizing the check is still worth it.)

- [ ] **`adapter-hook identity audit` skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror sync (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 21st report** — `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(wc -c ~/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(grep -n*)",
        "Bash(head * ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run; 21st consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`grep -o '...' *.jsonl`** — JSONL content extraction.

- [ ] **`grep -n '...' src/...`** — source file spot-checks.

- [ ] **`wc -l ~/.claude/projects/.../*.jsonl`** — transcript size check before parsing.

---

## Notes

- **Cron path mismatch is the single highest-ROI fix — 21st consecutive session.** CronDelete + CronCreate takes ~30 seconds and eliminates 2–3 wasted tool calls per nightly run. This is the #1 unresolved infrastructure issue.

- **HEAD detached again (20/21 sessions).** Root cause: remote containers clone at a commit hash, not at a named branch ref. Fix (`git checkout main && git pull origin main`) is simple but must precede any writes. CLAUDE.md rule still missing after 20 reports.

- **Jul 8–10 reviews confirmed on `origin/main`.** Pull fast-forwarded successfully — prior sessions' commit workflows worked end-to-end despite detached HEAD.

- **Repo quiet.** No code commits since PR #11 merge (`dc8cafa`). No open PRs. All Jul 10 carry-overs unresolved.

- **`!data.set_order` bug still open — confirmed today.** `src/routes/api.workout-sets.ts:41` — falsiness check rejects valid `set_order: 0`. One-line fix: change `!data.set_order` to `data.set_order == null`.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 19.** `docs/plans/` is active. `git mv` is the fix.

- **`.dev-server.log` still not gitignored — 17th report.** Lowest-effort open item. One line to `.gitignore`.

- **`docs/handoffs/` absent — day 14.** Commit `c748069` references a file there. `docs/reviews/` is the better home; consider adding a note to `docs/reviews/README.md` if one exists.

- **Three uncovered private API routes.** `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` still lack `assertUnauthorized` smoke coverage.

- **`graphify update .` overdue.** PRs #4–#11 unrepresented. Graph-based navigation unreliable for current codebase shape. Needs to run from a machine with the package installed.

- **No inline `useQuery` key arrays found.** Spot-check (`grep -rn "useQuery\(\[" src/`) returned 0 results — query-key factory pattern is being respected.
