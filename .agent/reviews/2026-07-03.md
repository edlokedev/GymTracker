# Nightly Review — 2026-07-03

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — 29 lines, this bootstrap session only). No historical transcripts available in cloud environment. Analysis based on current-session tool-use trace, git log, carry-over items from Jul 2 report, and repo state inspection._

**HEAD status:** Detached again on session start (13th consecutive occurrence). `git checkout main && git pull origin main` run before this commit. Pull fast-forwarded local main by 5 commits (Jul 1 fix + Jul 1/Jul 2 reviews).

**Orphan situation:** Jul 2 review (`87c8436`) confirmed on `origin/main` — Jul 2 session successfully pushed. We are now current. No orphans.

**Cron path mismatch still active:** This session's triggering prompt still contained the old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. The workflow file on `main` has the correct path (`-home-user-GymTracker/`) but the live cron job has not been re-created with the updated prompt. Every session until then wastes tool calls on path discovery.

**Activity since Jul 2:** No new code commits. Repo is quiet.

---

## Proposed CLAUDE.md Additions

### New this report

- [ ] **Nightly cron prompt uses old Windows path — re-create cron (NEW action item)** — This session received `C--Users-Hii-Documents-Code-GymTracker/` in its trigger prompt despite the workflow file on `main` being correct. The live cron must be deleted and re-created from the corrected `.agent/workflows/nightly-review.md`. Add reminder to §Git or §Shell: "After updating `.agent/workflows/nightly-review.md`, immediately delete and re-create any CronCreate jobs that use its prompt — the live cron is NOT auto-updated when the file changes."

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (13 reports, P0)** — `graphify-out/` is in `.gitignore`; it is never present in remote/cloud sessions. §Read First "always use the graph" still unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (13 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (2 reports, P0)** — Local branch lags remote by all commits from prior sessions. Without the pull, any push will conflict. Add to §Git.

- [ ] **No `.claude/settings.json` exists (13 reports, P0)** — Note: `.claude/` is now gitignored (line in `.gitignore`). File must be created per-machine. Add to §Shell or §Commands: "Create `.claude/settings.json` on each new machine/container with the standard `permissions.allow` allowlist before running any agentic session."

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useAuth()` called `adaptUser()` on every render. Rule: any hook that transforms context data must memoize on the stable source reference (`useMemo(() => adaptUser(supa.user), [supa.user])`).

- [ ] **`docs/handoffs/` absent — day 7** — `c748069` commit message references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. Commit the doc or remove the convention.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 11** — Both confirmed at repo root. Correct location: `docs/plans/`. `git mv` is the fix.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (9 reports)** — `.gitignore` has `server.log` and `server.err.log` but not `.dev-server.*`. Add both to `.gitignore`.

- [ ] **`status.md` / `decisions.md` location (11 reports)** — Live in Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage rule for private routes (11 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage. Add rule to §Testing.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Add to §Shell: "In remote sessions substitute Codex with an isolated-context `Agent` tool call."

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }` when full set is in memory.

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper or the filter view goes stale.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Validate `file.size ≤ 5 MB` before upload.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check when a component is used in multiple contexts.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 13th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in the cloud environment. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions. This is blocking the memory-audit section of every report.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge work. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day (`c748069`). If memory tracks production incidents, add this.

- [ ] **New API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE), custom exercises API, Vercel Analytics added. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 2nd report)** — Delete the live cron job that was created with the old Windows path and re-create it from the corrected `.agent/workflows/nightly-review.md`. The file has been correct on `main` since `7806280` (Jun 30). Until the cron is re-created, every nightly session receives the wrong path and must spend tool calls discovering the correct one.

- [ ] **`grill-with-docs` workflow (13 consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase.

- [ ] **Pre-commit branch check hook or step** — Add as step 0 to `gymmie-verification` skill: `git symbolic-ref HEAD` → if detached, abort and checkout main + pull first.

- [ ] **Adapter-hook identity audit skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — Update `nightly-review.md` with the distinction.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 13th report** — Note: `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run, 13th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`cat ~/.claude/projects/-home-user-GymTracker/*.jsonl`** — transcript reads during nightly review.

- [ ] **`ls /home/user/GymTracker/.agent/reviews/`** — directory listing during report compilation.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **`.claude/` is gitignored** (confirmed from `.gitignore`). This means `settings.json` cannot be committed to the repo. The standard allowlist must be applied to each new cloud container or machine manually, or via a session-start hook. This resolves the question of why it keeps being missing — it's by design, but the manual setup step is never documented.

- **Cron prompt mismatch is the single highest-ROI fix remaining.** Confirmed for the 2nd consecutive report. The workflow file is correct on `main`. The user just needs to delete the old cron and re-create it.

- **No new code today.** Repo is quiet. All open items carry forward unchanged.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 11.** `docs/plans/` already exists.

- **`docs/handoffs/` absent — day 7.** Directory does not exist; `c748069` commit message is broken.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- **`.dev-server.log` / `.dev-server.err.log` confirmed missing from `.gitignore` (9th report).** One-line fix.
