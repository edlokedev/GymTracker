# Nightly Review — 2026-06-28

_Transcript scan note: **8th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container. Only 1 JSONL at `~/.claude/projects/-home-user-GymTracker/` — the current bootstrap session (23 lines). No memory directory accessible. Analysis based on git log since Jun 27, code delta (none — quiet day), and carry-over items from Jun 27 report._

_Activity since Jun 27: **No new code commits.** Only commit is the nightly review itself (`9ace97b chore(agent): nightly review 2026-06-27`). Repository is in detached HEAD state (confirmed — `HEAD detached from refs/heads/main`). Quiet day; all open items carry forward unchanged._

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (unresolved from Jun 27, priority unchanged)

- [ ] **Object identity from adapter hooks must be memoized (ESCALATE — from c748069, production incident)** — `useAuth()` called `adaptUser()` on every render, building a fresh object each time. Consumers listing `user` in a dep array re-ran every render, driving an infinite edge-request loop (~280k Vercel function invocations/day, ~78% of Hobby cap). Fix: `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule: any hook that calls an adapter or constructor to transform context data must memoize its output on the stable source reference. Check: `adaptX(y)` or `new X(y.field)` in a hook body where the return is not wrapped in `useMemo` → flag.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — When the full dataset is already in memory, `runSearch` can short-circuit the API call and serve from in-memory state. Shape: `if (filters.favourites) { setExercises(state.favoriteExercises); setTotal(n); setHasMore(false); return; }`. Applicable only when: client has the full set (no pagination needed) and the filter is a view, not a new query. Revisit condition: if the dataset becomes paginated or unbounded, move server-side.

- [ ] **`reflectFavouritesIfActive` — mutation-aware filter view sync (carry-over)** — Every mutation path (toggle success, refresh) must call a `reflectXIfActive(newSet)` helper to update the displayed list, not just the underlying data. Without this, the filter view goes stale after mutation.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — With `togglingId: string | null`, disable ALL instances of the interactive control when `togglingId != null`. Rule: `disabled={togglingId != null}` on every control, `aria-busy={togglingId === thisId}` on the active one.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent UI wiring behind a mode check when a component is used in multiple contexts. Pattern: `!onSelectExercise` (absence of picker prop) = standalone mode; only wire stars, mutations, and auth-gated controls in standalone mode.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`. Mutate per-test inside `vi.mock`. Cleaner than re-mocking per test. Works because `vi.hoisted` runs before module evaluation.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — When testing disabled/busy state, pass a flag to the fetch stub that returns a never-resolving promise for the mutation endpoint. Pattern: `installFetch({ pendingMutation: true })` → POST/DELETE returns `new Promise(() => {})`.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount `<Analytics />` and `<SpeedInsights />` in `__root.tsx` body, outside `AuthProvider`, so unauthenticated and login routes are also tracked. Use `/react` exports (not a TanStack Start preset). One-time setup.

- [ ] **`docs/handoffs/` directory referenced but not committed (ESCALATE — day 2)** — `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md` referenced in commit `c748069` does not exist. Directory absent. Either commit the doc or drop the convention.

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (ESCALATE — 8 reports, P0)** — Still not added to CLAUDE.md. The §Read First "always use the graph" instruction wastes tool calls on every remote run. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately."

- [ ] **Detached HEAD in remote — must checkout before committing (ESCALATE — 7 reports, confirmed again today)** — Every remote session starts in detached HEAD. Add to §Git: "Remote/cloud sessions start in detached HEAD (`HEAD detached from refs/heads/main`). Run `git checkout main` or `git checkout -b <branch>` before branching or committing."

- [ ] **`status.md` / `decisions.md` location (ESCALATE — 6 reports)** — These live in the Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Plan file placement convention — PLAN.md / PLAN-REVIEW-LOG.md still at root (ESCALATE — 6 reports)** — Both files remain at repo root for the 6th consecutive review. Correct location: `docs/plans/custom-exercises.md` and `docs/plans/custom-exercises-PLAN-REVIEW-LOG.md`. This is a 2-minute commit.

- [ ] **Smoke test coverage rule for private routes (ESCALATE — 6 reports)** — `/api/exercises/custom` (GET/POST/PATCH/DELETE) and `/api/exercise-favorites` (GET/POST/DELETE) have no `assertUnauthorized` smoke coverage. Add to §Testing: "Every new `privateMethod` API route must have `{ expectStatus: 200 }` + `assertUnauthorized` in `scripts/smoke.ts` before the PR merges."

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` error code pattern from PR #8 undocumented. Add under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented. Add under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches. Add under §Architecture.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Do not call `supabase.rpc()` directly. Add under §Architecture.

- [ ] **`ConflictError` → 409 added to errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect` for one-time seeds. Add to §Architecture.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`. Add under §API + Auth or Testing.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (ESCALATE — 5+ reports)** — `.gitignore` covers `server.log`/`server.err.log` but not `.dev-server.*`. Add both patterns.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Add to §Shell: "In remote sessions substitute Codex Act 2 with an isolated-context `Agent` tool call."

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Add rule: validate `file.size ≤ 5 MB` client-side before `uploadCustomExerciseImage`.

---

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (8th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in the repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge commits. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day. If memory tracks production incidents, add this.

- [ ] **New routes/API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE) added in PR #9. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow (ESCALATE — 8 consecutive reports, P0)** — Confirmed used on at least 4 shipped features. Review log at `docs/plans/favourites-toggle-PLAN-REVIEW-LOG.md` shows the process in action: MAX_ROUNDS=5, REVISE/APPROVED loop, Codex-then-Claude fallback. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase.

- [ ] **Fix nightly-review workflow prompt path (ESCALATE — 8 reports, P0)** — `.agent/workflows/nightly-review.md` line 20 hardcodes `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` which has never resolved in 8 consecutive remote runs. The correct path is `~/.claude/projects/-home-user-GymTracker/`. Fix: update the prompt section to either hardcode the correct Linux path or instruct the agent to resolve via `ls ~/.claude/projects/` at runtime.

- [ ] **Adapter-hook identity audit skill (carry-over — from c748069)** — Recurring risk: hooks that build new objects from stable sources without `useMemo` cause infinite loops when consumers use the output in dep arrays. A small skill or checklist: grep for `useMemo`-less `adaptX(y)` or `new X(y.field)` calls in hook bodies.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — `docs/plans/` is confirmed home for feature plan files AND grill-with-docs review logs. `.agent/reviews/` = nightly self-reflection reports. Update `.agent/workflows/nightly-review.md` with this distinction.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` is a copy of `.agent/skills/gymmie-verification/`. The sync process and whether new skills must be mirrored are undocumented. Confirmed present today.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` exists in the project (ESCALATE — 8 reports)** — `permissions.allow` has never been configured. All Bash commands go through prompts. Create `.claude/settings.json` with at minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** (8th consecutive report) — Fire on every nightly review. Highest-priority permission candidates.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **No new code activity today.** All open items carry forward from June 27. The repo is quiet; no PRs opened or merged.

- **CRITICAL — fix the nightly-review workflow prompt (8th report, P0).** The Windows path encoding `C--Users-Hii-Documents-Code-GymTracker` has never resolved in any remote run. Correct path in `.agent/workflows/nightly-review.md` line 20 is `~/.claude/projects/-home-user-GymTracker/`. This single one-line fix eliminates 4–6 wasted tool calls and the transcript note on every run.

- **Detached HEAD confirmed again.** `git branch` shows `* (HEAD detached from refs/heads/main)` on session start. The nightly review commit (`9ace97b`) was made while detached — it attached to `main` only because `main` was already at that point. Future sessions that try to commit while detached and `main` has moved will silently create orphaned commits. Add the checkout rule to §Git.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — 6 days overdue.** Still at `/home/user/GymTracker/PLAN.md` and `/home/user/GymTracker/PLAN-REVIEW-LOG.md`. Correct paths: `docs/plans/custom-exercises.md` and `docs/plans/custom-exercises-PLAN-REVIEW-LOG.md`. 2-minute commit.

- **`docs/handoffs/` missing.** `c748069` references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md` in its commit message. Neither the directory nor the file exists. If handoff docs are not a committed convention, remove from future commit messages. If they are, create the directory and add it to §Source Layout.

- **`grill-with-docs` workflow still unwritten.** 8 consecutive flaggings. It is demonstrably established practice (4 shipped features). The workflow doc needs to exist before the next feature's plan phase starts — without it the process is rediscovered from scratch each time.

- **Open perf items (P6–P9, P12) unchanged** — SQL ilike/FTS, suggested-exercises full-catalog scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. Vercel Analytics added in `a9e3dbc` will now surface real performance data. Worth revisiting after a week of production traces.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage. A RLS regression on any of these could ship through CI silently.
