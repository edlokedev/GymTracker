# Nightly Review — 2026-07-05

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — this session only). No historical transcripts available in cloud environment. Analysis based on current-session tool-use trace, git log (PRs #10 and #11 merged today), carry-over items from Jul 4 report, and repo state inspection._

**HEAD status:** Detached again on session start (15th consecutive occurrence). `git checkout main && git pull origin main` run before this commit. Pull fast-forwarded local main by 8 commits (PR #10 TanStack Query Phase 5 + PR #11 exercise media mirror).

**Cron prompt mismatch still active — 15th session:** Prompt still uses old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. First `find` call failed; 4 additional discovery calls needed to locate `/root/.claude/projects/-home-user-GymTracker/`. Same as every prior session.

**Activity since Jul 4:** Two PRs merged. PR #10 completes TanStack Query migration. PR #11 self-hosts exercise media.

---

## Proposed CLAUDE.md Additions

### New this report

- [ ] **`queryKeys` centralized factory is now the only valid cache-key source (NEW)** — `src/lib/api/query-keys.ts` exports a `queryKeys` object with hierarchical array factories for every domain (calendar, workoutSessions, workoutSets, exercises, workoutTemplates, progress, locations). ADR-0007 mandates this. Add to §Architecture: "Never construct TanStack Query keys as inline arrays at call sites — always use `queryKeys.<domain>.<factory>()` from `src/lib/api/query-keys.ts`. Partial-key invalidation is only guaranteed safe via these factories."

- [ ] **`test/queryWrapper.tsx` — mandatory wrapper for any hook/component test touching TanStack Query (NEW)** — `createTestQueryClient()` (no retry, gcTime=Infinity) + a fresh `QueryClientProvider` per test. Forgetting this causes cache bleed between tests and hidden retry delays. Add to §Testing: "All hooks or components that call `useQuery`/`useMutation` must wrap in `createTestQueryClient()` from `test/queryWrapper.tsx` per ADR-0007."

- [ ] **Feature hook decomposition rule (NEW from PR #10)** — `useExerciseLibrary.ts` was split into `useExerciseFilters`, `useExerciseSearch`, `useExerciseQuickPicks`, `useFavoriteExercises`. Formalize the threshold: when a feature hook exceeds ~200 lines or manages 3+ independent concerns, split into focused sub-hooks. Add to §Architecture or §Source Layout.

- [ ] **Auth guard inconsistency is a known defect — three active treatments (NEW from frontend audit)** — The Jul 4 frontend fundamentals audit (`docs/reviews/2026-07-04-frontend-fundamentals-audit.md`) found three different auth guard patterns across private routes (skeleton+LoginPage / dead-end message / no guard at all). Add to §API + Auth: "Known defect: auth guard pattern is inconsistent across routes. Do not introduce a fourth approach — converge on the `LoginPage` render pattern used in `index.tsx:37-39` until a shared `RequireAuth` guard is built."

- [ ] **`/workout` dead-end bug + `GlobalErrorBoundary` dead code — do not regress (NEW from audit)** — Criticals from the Jul 4 audit still unaddressed. `/workout` shows no sign-in control for unauth users; `GlobalErrorBoundary` exists but is not registered anywhere. Add as known-open items so future agents don't touch those files without resolving the criticals.

- [ ] **Side effect inside React state updater is a bug — banned pattern (NEW from audit)** — Audit found network fetches, DOM focus, and nested `setState` calls inside `setState` updater functions across multiple files (`useWorkoutSession.ts:518-549`, `useCalendarData.ts:96-113`, `WorkoutSessionManager.tsx:202-292`). Add to §Style or §Architecture: "Never put effects (network calls, `setState`, DOM ops) inside a `setState` updater function — StrictMode double-fires updaters and React 19 may batch unpredictably. Compute the next value first, then set state, then run effects."

- [ ] **Transactionless delete-all-then-insert banned for multi-row writes (NEW from audit)** — `workout-templates.ts:284-305` can permanently empty a template on mid-write failure. Rule: multi-row mutations must use a SQL RPC (like the existing `repoint_workout_exercise`) or a transaction, never a loop of deletes + inserts. Add to §Data.

- [ ] **URL-rewrite migration pattern (NEW from PR #11)** — When media URLs or static references must change in existing rows, create a dedicated migration file (`UPDATE ... SET url = REPLACE(url, old_prefix, new_prefix)`). PR #11 used two sequential migrations (`20260705000000_` and `20260705010000_`). Add to §Data as "data URL rewrite" pattern.

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (15 reports, P0)** — §Read First "always use the graph" still unqualified for remote sessions.

- [ ] **Detached HEAD on every remote session start (15 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (4 reports, P0)** — Local branch lags remote by all commits from prior sessions.

- [ ] **No `.claude/settings.json` exists (15 reports, P0)** — `.claude/` is gitignored. Must be created per-machine/container. No permission allowlist active.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useMemo(() => adaptUser(supa.user), [supa.user])` pattern.

- [ ] **`docs/handoffs/` absent — day 9** — `c748069` references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. Note: `docs/reviews/` now exists — consider using it instead.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 13** — Correct location: `docs/plans/`. `git mv` needed.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (11 reports)** — One-line fix in `.gitignore`.

- [ ] **`status.md` / `decisions.md` in Odin workspace, not repo (13 reports)** — Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage for private routes (13 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Substitute with isolated `Agent` tool call.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }`

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — Validate `file.size ≤ 5 MB` in `CustomExerciseForm.tsx`.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check.

- [ ] **`!data.set_order` rejects set_order 0 bug (NEW from audit, open)** — `api.workout-sets.ts:41` — falsiness check rejects a valid `set_order: 0`.

- [ ] **`parsePaginationParams` shared helper missing (NEW from audit, open)** — Pagination parsing is inconsistent across route handlers; `api.exercises.recent.ts` clamps 1–50, others accept NaN/unbounded.

- [ ] **Body validation: adopt zod pattern for all mutating routes (NEW from audit, open)** — `api.exercises.custom.ts` uses zod; other mutating routes blind-cast `await request.json()`. Apply the zod pattern uniformly.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 15th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in the cloud environment. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions.

- [ ] **Graphify stale — 100+ commits since last update** — PR #4 through PR #11 all unrepresented. Graph is unreliable for current codebase shape.

- [ ] **API surface stale** — `src/lib/api/query-keys.ts` is new; all TanStack Query hooks are new or migrated. Any memory entry tracking the API surface is stale.

- [ ] **Production incident log** — useAuth memoization bug (c748069) consumed ~78% of Vercel Hobby 1M edge-request cap. Add if memory tracks production incidents.

- [ ] **`docs/reviews/` now exists** — `docs/handoffs/` pattern may be superseded. Update any memory entry about review/handoff conventions.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 15th report)** — Live cron still uses old Windows path. Workflow file on `main` has been correct since `7806280`. Fix: CronDelete old job, CronCreate from `.agent/workflows/nightly-review.md` (which has the correct `-home-user-GymTracker/` path). Costs: 4–5 wasted tool calls per nightly session until fixed.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (P1, 2nd report)** — `python3 -c` and `node -e` stdin piping both silently return empty output against the remote JSONL format (which contains non-message `type: "queue-operation"` / `type: "deferred_tools_delta"` entries). Reliable extraction: `grep -o '"command":"[^"]*"'` for Bash inputs, `grep -o '"name":"[^"]*"'` for tool names. Update `.agent/workflows/nightly-review.md` to use grep-based parsing, or add a `Read` call followed by parsing via the Read tool which handles the format natively.

- [ ] **`gymmie-verification` skill: add detached HEAD pre-check (15th report)** — Step 0: `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` before proceeding.

- [ ] **`grill-with-docs` workflow (15 consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md`.

- [ ] **`adapter-hook identity audit` skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`require-auth-guard` refactor workflow** — The frontend audit identified 3 inconsistent auth guard patterns. A dedicated workflow to audit and converge all private routes to a shared `RequireAuth` guard would be a clean bounded task.

- [ ] **`query-key-audit` skill** — After TanStack Query migration is complete, verify no inline cache-key arrays remain at call sites (should all be `queryKeys.*` factories). One grep pass: `grep -rn "useQuery\(\[" src/`.

- [ ] **`.codex/skills/` mirror sync (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 15th report** — `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(wc -c ~/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(head * ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run, 15th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`grep -o '...' *.jsonl`** — JSONL content extraction; more reliable than python3/node for this format.

- [ ] **`wc -l ~/.claude/projects/.../*.jsonl`** — transcript size check before parsing.

---

## Notes

- **Cron prompt mismatch is the single highest-ROI fix — 15th consecutive session.** Workflow file on `main` is correct. The live cron trigger has not been re-created. CronDelete + CronCreate takes ~30 seconds.

- **PR #10 (TanStack Query Phase 5) merged.** This is a large structural change: centralized `queryKeys` factory, feature hook decomposition (`useExerciseFilters`, `useExerciseSearch`, `useExerciseQuickPicks`, `useFavoriteExercises`), `test/queryWrapper.tsx` test infrastructure, ADR-0006 and ADR-0007 committed. Several CLAUDE.md additions proposed above to codify these patterns.

- **PR #11 (exercise media mirror) merged.** Exercise media now self-hosted at `edlokedev/gymmie-exercise-media` pinned to commit `19268b2`. CLAUDE.md was updated in this PR. Two sequential data-URL-rewrite migration files landed.

- **`docs/reviews/` now exists** — the `docs/handoffs/` convention from `c748069` may be obsolete. `docs/reviews/` is a more consistent location.

- **Frontend fundamentals audit (Jul 4) surfaced 2 Criticals and ~10 Important findings** — none addressed yet. `/workout` auth dead-end and `GlobalErrorBoundary` registration are the highest-priority unresolved code bugs.

- **`PLAN.md` / `PLAN-REVIEW-LOG.md` at root — day 13.** `docs/plans/` is active. One-liner fix.

- **`.dev-server.log` still not gitignored — 11th report.** Lowest-effort open item.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` still have no `assertUnauthorized` smoke coverage.

- **`graphify update .` is overdue.** PRs #4–#11 unrepresented. Any graph-based navigation is unreliable for current codebase shape.
