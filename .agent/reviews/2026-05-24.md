# Nightly Review — 2026-05-24

## Proposed CLAUDE.md Additions

- [ ] **RTK prefix mandatory** — Every Bash command must use `rtk` prefix (e.g. `rtk git status`, `rtk bunx tsc --noEmit`, `rtk bun run test`). Hook may auto-rewrite but explicit prefix required. Currently only in memory file, not enforced in CLAUDE.md.
- [ ] **PowerShell ban is hard** — Despite "never PowerShell" in CLAUDE.md, it was used multiple times during route migration session. Add explicit note: "Bash tool only. `PowerShell` tool is banned. No exceptions unless user types /powershell."
- [ ] **Windows temp paths** — `/tmp` and `/dev/stdin` are unreliable in git-bash on Windows. Use `$USERPROFILE/AppData/Local/Temp` or `mktemp -t` for temp files. Use file-based node piping (write to temp file first, then read).
- [ ] **Python unavailable** — `python3` / `python` not on PATH. Use `node -e` for JSON parsing and scripting. Never assume Python.
- [ ] **Node stdin piping** — `cmd | node -e "..."` with `process.stdin` works. `cmd > /tmp/x.json && node -e "require('/tmp/x.json')"` does NOT — `/tmp` maps to `C:\tmp` which doesn't exist. Use `process.stdin` pattern only.

## Memory Updates Needed

- [ ] **feedback_shell.md** already updated with RTK prefix rule ✓
- [ ] **project_context.md** — Add note about graphify Windows invocation: `PY="C:/Users/Hii/AppData/Roaming/uv/tools/graphifyy/Scripts/python.exe" && $PY -m graphify ...` (not `graphify` directly due to PATH issues).
- [ ] **Add feedback_powershell.md** — Dedicated memory for the PowerShell ban since it was violated twice in one session. Needs stronger signal.

## New Skills / Workflows

- [ ] **windows-shell-patterns** skill — Windows git-bash gotchas: `/tmp` → use process.stdin; `python3` → use node; `cd X && cmd` → prefer absolute paths; `$USERPROFILE` vs `~` behavior. High-value given repeated friction.
- [ ] **rtk-prefix-check** — Pre-commit or pre-response checklist: did every Bash command use `rtk` prefix? Could be a PreToolUse hook on Bash that reminds if `rtk` is missing.

## Permission Prompts to Reduce

- [ ] `Bash(bun run test *)` — 20 hits across sessions. Tests don't write to Supabase in current local-SQLite setup. Worth allowlisting. Reassess after Supabase migration completes.
- [ ] `Bash(rtk git *)` — Now that RTK prefix is mandatory, existing `git *` auto-allowlist won't match `rtk git *`. May need explicit `Bash(rtk git *)` or `Bash(rtk *)` — but `Bash(rtk *)` is too broad (covers `rtk bun run build`). Add specific forms: `Bash(rtk git status *)`, `Bash(rtk git log *)`, `Bash(rtk git diff *)`.
- [ ] `Bash(rtk bunx tsc --noEmit *)` — existing `Bash(bunx tsc --noEmit *)` won't match RTK-prefixed version.

## Notes

- **Auto-mode + settings.json**: Auto mode hard-blocks writes to `.claude/settings.json`. Requires non-auto mode for any settings changes. Consider documenting this in CLAUDE.md so it's not a surprise.
- **CronCreate durability**: `durable: true` param didn't actually persist to disk. Session-only behaviour confirmed. The UserPromptSubmit hook workaround (this session) is the fix.
- **vite.config.ts JSX runtime**: Took ~15 iterations to fix dev JSX runtime leaking into prod build. Root cause was TanStack Start's own esbuild transform running after Vite's React plugin. Resolved by explicit `@vitejs/plugin-react` mode override. Worth an ADR.
- **Hook fired correctly today**: UserPromptSubmit sentinel hook fired on first message, CronCreate re-registered successfully. System working.
