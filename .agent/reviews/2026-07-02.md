# Nightly Review — 2026-07-02

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — 25 lines, this bootstrap session only). No historical transcripts available. Analysis based on current-session tool-use trace, git log, carry-over items from Jul 1 report, and repo state inspection._

**HEAD status:** Detached again on session start (12th consecutive occurrence). `git checkout main && git pull origin main` run before this commit.

**Orphaned commits RESOLVED.** Remote `origin/main` is now at `959a730` (Jul 1 fix commit). `git pull origin main` fast-forwarded local `main` by 5 commits: Jun 29, Jun 30, Jul 1 reviews plus the `nightly-review.md` path fix are all on main. The multi-week orphan accumulation is cleared.

**nightly-review.md path fix is live.** The workflow file now has the correct path (`-home-user-GymTracker/`). However, the live cron that triggered today's session was created with the old Windows path (`C--Users-Hii-Documents-Code-GymTracker/`). The cron itself must be deleted and re-created with the updated prompt for the fix to take effect end-to-end.

**Activity since Jul 1:** No new code commits. Repo is quiet.

---

## Proposed CLAUDE.md Additions

### New this report

- [ ] **`git pull origin main` after `git checkout main` is mandatory (validated)** — Today confirmed: after `git checkout main`, local `main` was 5 commits behind `origin/main`. Without `git pull`, any commit would diverge from remote. The P0 rule from Jul 1 is correct. Add to §Git: "In remote sessions, always run `git pull origin main` immediately after `git checkout main` — the local branch lags the remote by all commits from prior sessions."

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (12 reports, P0)** — §Read First "always use the graph" still not qualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (12 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD` to check; if detached, run `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **No `.claude/settings.json` exists (12 reports, P0)** — `permissions.allow` never configured. Every Bash call goes through prompts. Create with the allowlist from the Jul 1 report.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useAuth()` called `adaptUser()` on every render. Rule: any hook that transforms context data must memoize on the stable source reference (`useMemo(() => adaptUser(supa.user), [supa.user])`).

- [ ] **`docs/handoffs/` absent — day 6** — `c748069` commit message references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. Commit the doc or remove the convention.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 10** — Both confirmed at repo root; correct location is `docs/plans/`. 2-minute `git mv`.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored — 9 reports** — `.gitignore` line 48 has `server.log` but not `.dev-server.*`. Add both entries.

- [ ] **`status.md` / `decisions.md` location — 10 reports** — These live in the Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage rule for private routes — 10 reports** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage. Add rule to §Testing.

- [ ] **Codex CLI unavailable in remote — carry-over Jun 21+** — Add to §Shell: "In remote sessions substitute Codex with an isolated-context `Agent` tool call."

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }` when full set is in memory.

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper or the filter view goes stale.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Validate `file.size ≤ 5 MB` before upload.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check when a component is used in multiple contexts.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 12th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in-repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Orphaned commit history — RESOLVED** — Jun 29/30 reviews and the `nightly-review.md` path fix are now on `origin/main`. Any memory entry tracking orphaned commits should be cleared.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge work. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day (`c748069`). If memory tracks production incidents, add this.

- [ ] **New API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE), custom exercises API, Vercel Analytics added. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0)** — The workflow file `.agent/workflows/nightly-review.md` is now fixed on `main`. But the live cron was created with the old prompt containing `C--Users-Hii-Documents-Code-GymTracker/`. Delete the old cron and re-create it using the prompt from the corrected workflow file. Until this is done, every nightly session wastes tool calls on path discovery.

- [ ] **`grill-with-docs` workflow — 12 consecutive reports, P0** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase.

- [ ] **Pre-commit branch check hook or step** — Add as step 0 to gymmie-verification skill: `git symbolic-ref HEAD` → if detached, abort and checkout main + pull first.

- [ ] **Adapter-hook identity audit skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — Update `nightly-review.md` with the distinction.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 12th report** — Create with `permissions.allow`. Minimum set:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** — fire on every nightly run, 12th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** — needed at session start before every commit.

- [ ] **`git pull origin main`** — needed right after `git checkout main`; newly validated today.

- [ ] **`git branch --contains <sha>`** — used in prior sessions to diagnose orphaned commits.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **Orphaned commits fully resolved.** `origin/main` is at `959a730` (Jul 1 fix). Local `main` pulled cleanly. Today's report will be the first in the normal run since Jun 28. The multi-session orphan chain is closed.

- **`git pull origin main` validated as P0.** Local `main` was 5 commits behind remote on session start. The rule from Jul 1 is confirmed correct: checkout alone is not enough in remote sessions.

- **Cron prompt still uses old Windows path.** The workflow file fix is on main, but the live cron trigger hasn't been re-created. Path discovery waste will recur on every session until the cron is regenerated.

- **No new code today.** Repo is quiet. All code carry-over items unchanged.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 10.** `docs/plans/` is active; `git mv` is the fix.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- **`docs/handoffs/` absent — day 6.** Easiest resolution: create the directory and commit the handoff doc referenced in `c748069`.
