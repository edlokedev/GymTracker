# Nightly Review — 2026-07-01

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — 41 lines, the current bootstrap session only). No historical transcripts available. Analysis based on current-session tool-use trace, git log, and carry-over items from Jun 30 report._

**HEAD status:** Detached again on session start (`git symbolic-ref HEAD` → DETACHED). 11th consecutive occurrence. `git checkout main` run before this commit.

**CRITICAL new finding:** Commits `7806280` (path fix), `622476b` (Jun 30 review), `28aeabf` (Jun 29 review) are ORPHANED — committed in detached HEAD in previous sessions, never merged to `main`. `git branch --contains 7806280` returns empty. `main` is last at `c9f2c1e` (Jun 28 review). Three nightly review reports and the path fix are invisible to anyone looking at the repo.

**Activity since Jun 30:** No new code commits on `main`. Quiet day.

---

## Proposed CLAUDE.md Additions

### New this report

- [ ] **CRITICAL — commit BEFORE `git checkout main`, never after (ROOT CAUSE of orphaned commits)** — Sessions start detached; previous runs committed in detached HEAD then called `git checkout main`, orphaning the commits. Correct sequence: (1) `git symbolic-ref HEAD` to check, (2) if detached `git checkout main`, (3) then make changes and commit. Add to §Git: "Always run `git checkout main` before editing any file you intend to commit, not after. Commits made in detached HEAD are orphaned and invisible on `main`."

- [ ] **Orphaned nightly reviews — cherry-pick needed** — Jun 29 (`28aeabf`), Jun 30 (`622476b`), and the path fix (`7806280`) are not on `main`. Either cherry-pick all three in order or accept the loss and note it. Requires explicit user decision.

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (11 reports, P0)** — §Read First "always use the graph" still not qualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (11 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD` to check; if detached, `git checkout main` BEFORE making any changes."

- [ ] **No `.claude/settings.json` exists (11 reports, P0)** — `permissions.allow` never configured. Every Bash call goes through prompts. Create with the allowlist from the Jun 30 report.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useAuth()` called `adaptUser()` on every render; ~280k Vercel edge invocations/day. Fix: `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule: any hook that transforms context data must memoize on the stable source reference.

- [ ] **`docs/handoffs/` absent — day 5** — `c748069` commit message references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. Commit the doc or remove the convention.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 9** — Both confirmed at repo root; correct location is `docs/plans/`. 2-minute commit.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored — 8 reports** — `.gitignore` has `server.log` (line 48) but not `.dev-server.*`. Add both entries.

- [ ] **`status.md` / `decisions.md` location — 9 reports** — These live in the Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage rule for private routes — 9 reports** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage. Add rule to §Testing.

- [ ] **Codex CLI unavailable in remote — carry-over Jun 21+** — Add to §Shell: "In remote sessions substitute Codex with an isolated-context `Agent` tool call."

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }` when full set is in memory.

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper or the filter view goes stale.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Validate `file.size ≤ 5 MB` before upload.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check when a component is used in multiple contexts.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 11th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in-repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Orphaned commit history** — If memory tracks "last nightly review committed", it should note that Jun 29 / Jun 30 reviews and the path fix (`7806280`) are orphaned from `main`.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge work. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day (`c748069`). If memory tracks production incidents, add this.

- [ ] **New API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE), custom exercises API, Vercel Analytics added. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **CRITICAL — fix the nightly-review cron trigger (NEW P0)** — The workflow file `.agent/workflows/nightly-review.md` was corrected in orphaned commit `7806280`, but the cron trigger that fires this session was created with the old prompt (`C--Users-Hii-Documents-Code-GymTracker/`). Today's session still wasted 4 tool calls discovering the path. The fix requires: (1) cherry-picking `7806280` to main, (2) deleting and re-creating the cron with the updated prompt from the fixed workflow file. The workflow file on `main` still has the OLD path — the fix is orphaned.

- [ ] **`grill-with-docs` workflow — 11 consecutive reports, P0** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase.

- [ ] **Pre-commit branch check hook or step** — Add as step 0 to gymmie-verification skill or as a standalone pre-commit reminder: `git symbolic-ref HEAD` → if detached, abort and checkout main first.

- [ ] **Adapter-hook identity audit skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — Update `nightly-review.md` with the distinction.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 11th report** — Create the file with `permissions.allow`. Minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** — fire on every nightly run, 11th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** — needed at session start before every commit.

- [ ] **`git branch --contains <sha>`** — used today to diagnose orphaned commits.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **ROOT CAUSE OF ORPHANED COMMITS IDENTIFIED.** Three prior nightly sessions committed in detached HEAD and then called `git checkout main` — that checkout does not bring the commits along, it leaves them as orphans. The correct sequence is checkout FIRST, commit SECOND. The `main` branch shows the last nightly review as Jun 28. Jun 29, Jun 30, and the path fix are only reachable via their SHA. This session commits today's report correctly (checkout was run before this commit).

- **Nightly-review workflow path fix (`7806280`) is on an orphan branch, not on `main`.** The workflow file on `main` still contains the old path `C--Users-Hii-Documents-Code-GymTracker/`. Until it is cherry-picked to main and the cron is re-created with the corrected prompt, each nightly run will waste 4+ tool calls on path discovery. Single-highest-ROI fix in the backlog for 11 consecutive reports.

- **No new code today.** Repo is quiet. All open items carry forward unchanged.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 9.** `docs/plans/` already exists and has active files; a simple `git mv` would close this.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- **`docs/handoffs/` absent — day 5.** `c748069` commit message references a file in a directory that doesn't exist. Easiest resolution: create the directory and commit the handoff doc.
