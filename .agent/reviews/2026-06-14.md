# Nightly Review — 2026-06-14

_Sessions scanned: 5f9ab66f (current accumulation session, grew 179KB Jun 9→14 — only the Jun 9 review content), 3e7384e3 (Jun 14 mtime touch, size unchanged — no new content), 159d621e (Jun 4), agent-a2c7106a38f947f7d (Jun 3), dd2fcb26 (Jun 2). No new feature sessions detected. No new commits since 2026-06-09 (HEAD: 2dc3cb6 "Collapse quick exercise picks on mobile"). 5-day quiet period. Only 1× review prompt today (down from 3× on Jun 7 and Jun 9 — possible stacking improvement or one-off)._

---

## Proposed CLAUDE.md Additions

- [ ] **`gymmie-verification` skill still not referenced in CLAUDE.md** — Carry-over from Jun 9 review. The skill exists at `.agent/skills/gymmie-verification/SKILL.md` and was committed Jun 7. The CLAUDE.md Commands section still lists the manual pre-handoff checklist. Update Commands to reference `/gymmie-verification` instead. Also: the skill's step 5 (`graphify update .` when routes/API/DB/features changed or ≥5 files) would fix the persistent graphify-stale pattern if the skill is actually invoked.

- [ ] **`.dev-server.log` and `.dev-server.err.log` are untracked and not gitignored** — `git status --short` shows `?? .dev-server.err.log` and `?? .dev-server.log`. The `.gitignore` covers `server.log` and `server.err.log` but NOT the dot-prefixed variants. Add `.dev-server.log` and `.dev-server.err.log` to `.gitignore` (or a glob `*.dev-server.log`). Low risk but these will appear in every `git status` until ignored.

- [ ] **Review files are untracked** — `.agent/reviews/` files from 2026-05-28 through 2026-06-09 are all `??` in `git status`. `2026-05-27.md` is modified (`M`). Either (a) reviews should be committed regularly as part of repo history, or (b) `.agent/reviews/` should be added to `.gitignore`. Document the intended treatment. If reviews are meant to be tracked, add a step to the review workflow: after saving the report, stage and commit it with `rtk git add .agent/reviews/ && rtk git commit -m "chore(reviews): ..."`.

- [ ] **Branch changed from `codex/supabase-vercel-migration-docs` to `main` mid-session** — At session start the active branch was `codex/supabase-vercel-migration-docs`. Current state is `main`. The switch happened silently at some point during the long 5f9ab66f accumulation session. No git checkout command was observed in the transcript tail. Worth documenting: "Review and maintenance sessions should confirm branch with `rtk git status --short --branch` before any commits. Do not assume branch state persists across context compactions."

## Memory Updates Needed

- [ ] **`project_context.md`** — Note the current branch is `main` (was `codex/supabase-vercel-migration-docs` at start of session). Add: "As of Jun 2026, active development is on `main`. `codex/supabase-vercel-migration-docs` branch exists but no new commits on it." (Report only.)

- [ ] **Memory files are otherwise current** — All 9 memory files updated within the last 7 days. No stale entries detected.

## New Skills / Workflows

- [ ] **`.codex/skills/` mirror** — Carry-over from Jun 9. `.codex/skills/gymmie-verification/SKILL.md` mirrors `.agent/skills/`. Purpose still undocumented. If `.codex/` is for a second AI toolchain, add a note to the skill-creation workflow: "New skills go in both `.agent/skills/<name>/SKILL.md` and `.codex/skills/<name>/SKILL.md`."

- [ ] **Review commit workflow** — Given that 8 review files are untracked, add a micro-workflow: after writing the nightly report, run `rtk git add .agent/reviews/YYYY-MM-DD.md && rtk git commit -m "chore(reviews): add YYYY-MM-DD nightly review"`. This keeps the review history in git rather than only on disk.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 12th consecutive day flagged. These are clearly safe to allowlist.
- [ ] **`Bash(netstat -ano *)`** — 12th consecutive day. Read-only. Allowlist candidate.

## Notes

- **5-day quiet period (Jun 9–14)** — No feature commits, no new sessions. The product is stable at: exercise discovery quick picks (with mobile collapse), history-based set prefill, saved workout templates, workout starts fix, WorkoutDashboard simplification. The progress dashboard PM analysis from Jun 3 is still the most recent identified next feature area.
- **Review stacking may be improving** — Only 1× review prompt fired today vs. 3× on Jun 7 and Jun 9. Unclear whether this is a real fix (user deduped RemoteTrigger registrations?) or a one-off. Monitor for next 2 sessions before declaring resolved.
- **Graphify still stale — 11 days** — Last run was before the Jun 3 sprint. Graph does not reflect: workout-templates feature, exercise discovery, set prefill, WorkoutDashboard simplification, or the mobile collapse changes. Once `gymmie-verification` skill is wired into CLAUDE.md and actually invoked, step 5 should trigger `graphify update .` on the next ≥5-file commit.
- **Three persistent items at 12 days**: `taskkill`/`netstat` allowlist, graphify stale, `gymmie-verification` not in CLAUDE.md. All require the user or a feature session to action — they won't self-resolve via reviews alone.
