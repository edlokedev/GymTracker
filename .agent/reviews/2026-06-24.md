# Nightly Review — 2026-06-24

_Transcript scan note: **5th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container. Only 1 JSONL found at `-home-user-GymTracker/` — the current bootstrap session (24 lines, no prior turns). No memory directory accessible. Analysis based on git log, code delta since Jun 23 review, and carry-over items from previous reports._

_Activity since Jun 23: **Zero code commits.** Only commit is `c5aa940` (the nightly review for Jun 23). No PRs opened or merged. Quiet day._

---

## Proposed CLAUDE.md Additions

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (ESCALATE — 5 reports)** — `graphify-out/` is gitignored and never present in remote/cloud sessions. CLAUDE.md §Read First step 5 says "Always use the graph before raw search", which is actively misleading in cloud runs. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately. The graph rule applies only to local desktop sessions where `graphify update .` has been run."

- [ ] **Detached HEAD in remote — must checkout before committing (ESCALATE — 4 reports)** — Every remote session starts with HEAD detached. The Git section should say: "Remote/cloud sessions start in detached HEAD. Run `git checkout main` (or `git checkout -b <branch>`) before branching or committing."

- [ ] **`status.md` / `decisions.md` location (ESCALATE — 3 reports)** — CLAUDE.md step 4 lists these as project state without clarifying they live in the Odin workspace root (parent of `GymTracker/`), not in this repo. Add: "These live in the Odin workspace root, not `GymTracker/`. Skip when running in remote sessions."

- [ ] **Plan file placement convention (ESCALATE — 3 reports, `PLAN.md` still at root)** — `PLAN.md` and `PLAN-REVIEW-LOG.md` remain at the repo root for the 3rd consecutive review day (now 3 days past first flag). Add to Architecture/Docs: "Feature plans → `docs/plans/<feature>.md`. Grill-with-docs logs → `docs/reviews/<feature>-review-YYYY-MM-DD.md`. Never use root-level `PLAN.md` / `PLAN-REVIEW-LOG.md`."

- [ ] **Smoke test coverage rule for `privateMethod` routes (ESCALATE — 3 reports)** — `/api/exercises/custom` (GET/POST/PATCH/DELETE) confirmed absent from `scripts/smoke.ts` (only `/exercises` page route and `/api/exercises/search` are covered). Add to Testing: "Every new `privateMethod` API route must have a `{ expectStatus: 200 }` + `assertUnauthorized` entry in `scripts/smoke.ts`."

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23)** — Confirmed in 3 locations: `seededCompletionSessionIdRef` (`WorkoutSessionManager.tsx:105`), `startedTemplateIdRef` (`useWorkoutSession.ts:97`), `lastAppliedRouteKey` (`useExerciseLibrary.ts:88`). Add one-liner under Architecture: "One-time seed effects use a ref guard (`useRef<SomeId | null>(null)`) — check and set the ref inside `useEffect` to run exactly once per entity load without putting the entity in the dep array."

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23)** — Add under API + Auth or Testing: "When passing an async model function to a `() => void` prop, discard the return at the call site: `onFoo={() => { void modelFn() }}` — don't pass the fn reference directly if the return type doesn't match."

- [ ] **`.playwright-mcp/` not gitignored (ESCALATE — 3 reports)** — Will reappear as untracked on the next Playwright MCP run. Add `.playwright-mcp/` to `.gitignore`.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (ESCALATE — 3+ reports)** — `.gitignore` covers `server.log`/`server.err.log` but not the `.dev-server.*` variants created by the dev-server workflow. Add both glob patterns.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21/22/23)** — `codex: command not found` for every remote feature run. Add to Shell or grill-with-docs workflow: "In remote sessions substitute Codex Act 2 with an isolated-context `Agent` tool call. Record as 'Substitute reviewer (isolated context)' in the review log."

- [ ] **Image upload file size validation missing (carry-over Jun 22)** — `CustomExerciseForm.tsx` validates MIME type but not file size. The locked plan specified a client-side size cap. Add: "`exercise-images` uploads must client-side-reject files above a size limit (recommend ≤5 MB). Validate `file.size` before calling `uploadCustomExerciseImage` — `<input accept='image/*'>` enforces type at the OS picker only."

---

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (5th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in the remote container. Memory audit cannot run here. Decide: (a) commit memory to `.agent/memory/` in the repo, or (b) document that memory audit is skipped in remote sessions.

- [ ] **Graphify stale — 70+ commits since last update** — If any memory entry marks the graph as current or useful, flag as stale. The graph misses all of PR #4 (custom exercises), PR #5 (calendar TZ), PR #6 (completion bar), PR #7 (app icon), plus all perf work.

---

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow (ESCALATE — 5 consecutive reports, now P0)** — Adversarial plan review has now run on at least 3 features without a documented workflow. Create `.agent/workflows/grill-with-docs.md` covering: header params (`MAX_ROUNDS`, `PLAN_FILE`, `LOG_FILE`), REVISE/APPROVED loop, convergence rule, lock phrase, and remote-fallback (Agent tool in `isolation: 'worktree'` instead of Codex CLI).

- [ ] **`docs/reviews/` vs `.agent/reviews/` naming (carry-over Jun 21/22/23)** — Two review file types coexist with no doc distinguishing them. Add a note in `.agent/workflows/nightly-review.md`: "`docs/reviews/` = grill-with-docs plan-review logs (task-scoped). `.agent/reviews/` = nightly self-reflection reports (date-scoped, automated)."

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` is a copy of `.agent/skills/gymmie-verification/`. The sync process (if any) and whether new skills must be mirrored are undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` exists in the project** — `permissions.allow` has never been configured. All Bash commands go through prompts. Create `.claude/settings.json` with at minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** (5th consecutive report) — These fire on every nightly review and are safe read-only ops. Highest-priority permission candidates.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — Not testable from the remote container but likely still prompting on the user's local machine. Candidates: `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **CRITICAL — fix the nightly-review workflow prompt (5th report, P0).** The path `C--Users-Hii-Documents-Code-GymTracker` is the Windows path encoding and has never resolved in any remote run. The fix in `.agent/workflows/nightly-review.md` prompt section should replace the hardcoded path with: `$(ls ~/.claude/projects/ | head -1)` or instruct the agent to run `ls ~/.claude/projects/` first and use the found directory. The current silent failure wastes 4–6 tool calls at the start of every run. **This is the single highest-impact change available today.**

- **Quiet day — no code activity since Jun 23.** No new PRs, commits, or feature work. All findings are carry-overs from previous reviews. The backlog of unresolved recommendations is now 3–5 days old for several items.

- **`PLAN.md` / `PLAN-REVIEW-LOG.md` cleanup is trivial and 3 days overdue.** Moving them to `docs/plans/custom-exercises.md` and `docs/reviews/custom-exercises-review-2026-06-22.md` is a 2-minute commit with zero code risk.

- **Smoke test gap is a real risk.** `/api/exercises/custom` (4 methods) has been live for 3 days with no `assertUnauthorized` coverage. A smoke run would silently pass while RLS regressions on these routes go undetected.

- **The `grill-with-docs` workflow has been flagged for 5 consecutive reviews without being created.** It has now been used for at least 3 shipped features and is de facto established practice. Prioritize creating the workflow doc before the next feature kicks off.

- **Open perf items (P6–P9, P12) unchanged** — SQL ilike/FTS, suggested-exercises full-catalog scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip. Scale-gated, low urgency.
