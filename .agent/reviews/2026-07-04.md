# Nightly Review — 2026-07-04

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — 33 lines, this bootstrap session only). No historical transcripts available in cloud environment. Analysis based on current-session tool-use trace, git log, carry-over items from Jul 3 report, and repo state inspection._

**HEAD status:** Detached again on session start (14th consecutive occurrence). `git checkout main && git pull origin main` run before this commit. Pull fast-forwarded local main by 7 commits (Jul 1/Jul 2/Jul 3 reviews plus `nightly-review.md` path fix and one root-cause correction).

**Cron prompt mismatch still active — 14th session:** Prompt received today still used old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. First `find` command failed, requiring 4 additional discovery tool calls to locate the correct path at `/root/.claude/projects/-home-user-GymTracker/`.

**Activity since Jul 3:** No new code commits. Repo is quiet.

**New observation this session:** `python3 -c` and `node -e` both produced no output when attempting to parse the JSONL transcript. The JSONL format contains non-standard `type: "queue-operation"` and `type: "deferred_tools_delta"` entries that are not `role`/`content` message objects. Naive role-based parsers silently skip all content. Fallback to `grep -o` patterns worked correctly.

---

## Proposed CLAUDE.md Additions

### New this report

- [ ] **JSONL transcript format has non-message entries — use grep not role-based parsers (NEW)** — The remote JSONL contains `type: "queue-operation"` and `type: "deferred_tools_delta"` entries alongside `role: "assistant"` message objects. Parsers that iterate over lines and look for `role` fields silently skip most content. The reliable extraction approach is `grep -o '"command":"[^"]*"'` for Bash inputs and `grep -o '"type":"tool_use"[^}]*"name":"[^"]*"'` for tool names. Add this to the nightly-review workflow prompt or as a §Commands note.

- [ ] **`python3` unavailable in remote containers — nightly-review script violated its own rule (NEW)** — CLAUDE.md §Shell already says "No `python` / `python3` — use `node -e`." This session's transcript-parsing attempt used `python3 -c` anyway and produced no output. Nightly review prompt must be updated to use only `grep -o` or `node -e` for JSONL parsing, and the `node -e` pipe must handle non-message JSON lines gracefully (wrap each parse in try/catch and skip lines where `.role` is undefined).

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (14 reports, P0)** — §Read First "always use the graph" still unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (14 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (3 reports, P0)** — Local branch lags remote by all commits from prior sessions. Without the pull, any push will conflict. Add to §Git.

- [ ] **No `.claude/settings.json` exists (14 reports, P0)** — Note: `.claude/` is gitignored (`.gitignore` line). File must be created per-machine. Add to §Shell or §Commands: "Create `.claude/settings.json` on each new machine/container with the standard `permissions.allow` allowlist before running any agentic session."

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useAuth()` called `adaptUser()` on every render. Rule: any hook that transforms context data must memoize on the stable source reference (`useMemo(() => adaptUser(supa.user), [supa.user])`).

- [ ] **`docs/handoffs/` absent — day 8** — `c748069` commit message references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. Commit the doc or remove the convention.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 12** — Both confirmed at repo root. Correct location: `docs/plans/`. `git mv` is the fix.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (10 reports)** — `.gitignore` has `server.log` and `server.err.log` but not `.dev-server.*`. Add both to `.gitignore`.

- [ ] **`status.md` / `decisions.md` location (12 reports)** — Live in Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage rule for private routes (12 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage. Add rule to §Testing.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Add to §Shell: "In remote sessions substitute Codex with an isolated-context `Agent` tool call."

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }` when full set is in memory.

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper or the filter view goes stale.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Validate `file.size ≤ 5 MB` before upload.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check when a component is used in multiple contexts.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 14th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist in the cloud environment. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions. Blocking the memory-audit section of every report.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge work. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day (`c748069`). If memory tracks production incidents, add this.

- [ ] **New API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE), custom exercises API, Vercel Analytics added. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 3rd report)** — The live cron job uses the old Windows path `C--Users-Hii-Documents-Code-GymTracker/` in its prompt. The workflow file on `main` has the correct path (`-home-user-GymTracker/`) since `7806280`. Delete the old cron (CronDelete) and re-create it (CronCreate) from the updated `.agent/workflows/nightly-review.md`. Every session until this is done wastes 4-5 tool calls on path discovery.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (NEW, P1)** — Replace python3/node-based JSONL parser in the prompt with: `grep -o '"command":"[^"]*"'` for bash inputs and `grep -o '"type":"tool_use"[^}]*"name":"[^"]*"'` for tool names. These work reliably against the remote JSONL format that includes non-message `type: "queue-operation"` entries.

- [ ] **`grill-with-docs` workflow (14 consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase.

- [ ] **Pre-commit branch check hook or step** — Add as step 0 to `gymmie-verification` skill: `git symbolic-ref HEAD` → if detached, abort and checkout main + pull first.

- [ ] **Adapter-hook identity audit skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — Update `nightly-review.md` with the distinction.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 14th report** — Note: `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(wc -c ~/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(head * ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run, 14th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`wc -c ~/.claude/projects/.../*.jsonl`** — used this session to check transcript byte size before parsing.

- [ ] **`grep -o '...' *.jsonl`** — used this session for JSONL content extraction; more reliable than python3/node for this format.

- [ ] **`head -5 ~/.claude/projects/.../*.jsonl`** — used for JSONL format inspection.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **Cron prompt mismatch is the single highest-ROI fix — 14th consecutive session.** Workflow file on `main` is correct. The live cron trigger has not been re-created. Every nightly session until this is fixed begins with a failed path lookup and 4-5 wasted tool calls. Fix takes ~30 seconds: CronDelete the old job, CronCreate from the corrected workflow file prompt.

- **JSONL parsing approach documented for the first time.** The remote JSONL format includes `type: "queue-operation"`, `type: "deferred_tools_delta"`, and `type: "agent_listing_delta"` entries that are not role/content message objects. `python3 -c` and `node -e` naive parsers silently return empty output. `grep -o` on specific JSON key patterns is the reliable extraction path in this environment.

- **`git pull` fast-forwarded by 7 commits today.** The prior sessions' review commits (Jul 1/2/3 + path fix + root-cause fix) are all now on `origin/main` and pulled clean. No orphan situation this session.

- **No new code today.** Repo is quiet. All code carry-over items unchanged.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 12.** `docs/plans/` already exists and is active.

- **`docs/handoffs/` absent — day 8.** `c748069` commit message references a file in a directory that doesn't exist. Easiest resolution: create the directory and commit the handoff doc, or drop the convention.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- **`.dev-server.log` / `.dev-server.err.log` still not gitignored (10th report).** One-line fix in `.gitignore`. Lowest effort open item in the entire backlog.
