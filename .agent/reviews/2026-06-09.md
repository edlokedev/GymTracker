# Nightly Review — 2026-06-09

_Sessions scanned: 5f9ab66f (current accumulation session — Jun 7 maintenance + Jun 9 review), 159d621e (Jun 4), 3e7384e3 + agent-a2c7106a38f947f7d (Jun 3), dd2fcb26 (Jun 2). New commits since last review (Jun 7): `4a0ab8c Fix workout starts and dashboard actions` (9 files) and `2dc3cb6 Collapse quick exercise picks on mobile` (2 files). Both made Jun 7 ~12:13–12:34 SGT, after the Jun 7 review report was saved — so these weren't captured in the prior review._

---

## Proposed CLAUDE.md Additions

- [ ] **`gymmie-verification` skill exists — CLAUDE.md should reference it** — `4a0ab8c` added `.agent/skills/gymmie-verification/SKILL.md` formalizing the pre-handoff verification workflow (format → lint → focused tests → build → `graphify update .` if ≥5 files). The CLAUDE.md Commands section still lists this as an ad-hoc checklist. Update Commands to read: "Run `/gymmie-verification` (`.agent/skills/gymmie-verification/SKILL.md`) instead of the manual checklist." Also note that the skill handles `graphify update .` automatically — closing the 10-day gap where graphify wasn't being run after large commits.

- [ ] **`.codex/skills/` — undocumented secondary skill directory** — `4a0ab8c` also committed `gymmie-verification` to `.codex/skills/gymmie-verification/SKILL.md`. This is a new directory not mentioned in CLAUDE.md or any workflow doc. Purpose is unclear — possibly for Codex CLI or a second AI coding assistant. Document in CLAUDE.md: "New skills should be placed in BOTH `.agent/skills/` and `.codex/skills/` — the `.codex/` tree mirrors `.agent/` for alternate toolchain compatibility."

- [ ] **`window.matchMedia` + `useEffect` for client-side responsive state** — `2dc3cb6` introduces the pattern of using `window.matchMedia('(max-width: 639px)')` + `addEventListener('change', ...)` + `useEffect` cleanup for reactive viewport-based UI state (the quick picks collapse toggle). This is not in CLAUDE.md Styling section, which only says "mobile-first" and "Tailwind utilities inline." Add a note: "For behavior that must switch at a breakpoint (collapse/expand, layout swap) and can't be done with Tailwind visibility classes alone — use `window.matchMedia` + `useEffect` with event listener cleanup. Guard with `typeof window === 'undefined'` for SSR safety."

- [ ] **`graphify update .` trigger met again, not run** — `4a0ab8c` touched 9 files across workout-history, workout-session, workout-templates, and routes. CLAUDE.md trigger: ≥5 files. The new `gymmie-verification` skill includes `graphify update .` as step 5 of the workflow — but the skill must be invoked for this to happen. Graph is now stale ≥6 days. Once the skill is wired into CLAUDE.md, this should self-correct.

- [ ] **`onDuplicated` callback removed from `useWorkoutHistory`** — The Jun 7 fix removed the `onDuplicated: (session) => navigate(...)` pattern from `WorkoutDashboard` (both instances). If this callback still exists in the `useWorkoutHistory` type, it's dead API. Check if it was removed from the hook signature too, or just the call sites. Update type definitions to match.

## Memory Updates Needed

- [ ] **`project_context.md`** — Add: "Two new commits on Jun 7 (post Jun 7 review): `4a0ab8c` (workout starts bugfix, WorkoutDashboard simplification, `useWorkoutHistory` refactor, new `gymmie-verification` skill) and `2dc3cb6` (mobile collapse for quick picks in ExerciseSelectorModal). These touched workout-session, workout-history, workout-templates, exercise-library, routes — graphify graph is now ≥6 days stale." (Report only — do not edit now.)

## New Skills / Workflows

- [ ] **`gymmie-verification` skill is active** — Created at `.agent/skills/gymmie-verification/SKILL.md` and `.codex/skills/gymmie-verification/SKILL.md`. Invoked via `/gymmie-verification`. Workflow: format → lint → focused tests → build → optionally `graphify update .`. This replaces the manual pre-handoff checklist in CLAUDE.md. **Action: update CLAUDE.md to reference the skill.**

- [ ] **Mobile collapse pattern for tall sections** — `2dc3cb6` introduces a reusable pattern: detect compact viewport on mount, default to collapsed, toggle via a summary button with `aria-expanded`. The `getIsCompactViewport` helper + `matchMedia` + state init is a short but non-obvious snippet. Could be extracted into a `useCompactViewport()` hook or documented in `.agent/skills/styling/` for reuse in similar mobile sections.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 11th consecutive day. No new code adds this risk. Strong candidate for allowlist.
- [ ] **`Bash(netstat -ano *)`** — 11th consecutive day. Read-only. Strong candidate for allowlist.

## Notes

- **3× review stacking persists (Jun 9)** — Fired 3× simultaneously again. No investigation of RemoteTrigger registration count has been done. This is the highest-priority operational issue still unresolved. Needs the user to check how many remote schedules are registered and deduplicate.
- **Jun 7 commits missed by Jun 7 review** — Both new commits (12:13 and 12:34 SGT Jun 7) happened after the review report was saved that morning. This is expected — the review captures state at run time, not end-of-day.
- **`gymmie-verification` skill is the biggest positive change** — Formalizes 10+ days of "pre-handoff checks skipped" friction into an invocable skill. The skill even includes `graphify update .` as a conditional step. Once CLAUDE.md is updated to reference it, the most persistent friction patterns (skipped checks, stale graph) should resolve.
- **WorkoutDashboard simplification** — `onDuplicated` callback and "Repeat Last Workout" button removed. `startRecommendedWorkout` now navigates directly instead of calling `duplicateSession`. Simpler flow — likely fixes a bug where duplicate sessions were being created unexpectedly when starting from a template.
- **No new feature sessions in top 5** — The Jun 7 feature work (commits at 12:13/12:34) likely happened in the same `5f9ab66f` session in the early compacted context. The session file is 4MB and the feature work is in the first ~3.8MB. Review tooling only scans tails — the commits were caught via `git log` instead.
