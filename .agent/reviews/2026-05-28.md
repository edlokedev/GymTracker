# Nightly Review — 2026-05-28

_Sessions scanned: 5f9ab66f (nightly review session, current), agent-a60ab42f9dac9a225 (new Explore subagent), agent-ade629c0f0e8ee14b (yesterday's Explore subagent), 159d621e (review-findings application + mobile UI session), b0b7ba53 (older migration work). Context: 159d621e applied most 2026-05-27 findings (bc9b038 commit) then made 10 mobile UI commits in the same session._

---

## Proposed CLAUDE.md Additions

- [ ] **Nightly review fires 3× simultaneously** — Three identical queue-operation enqueues fired within 3ms of each other (2026-05-28T08:37:10.177/179/180Z). Likely a cron stacking issue: each new session calls CronCreate without checking for an existing registration. Although CronCreate jobs are session-only, if multiple sessions overlap or if the hook fires multiple times, crons accumulate. Fix: the startup hook should emit the CronCreate call only once. Or: check `CronList` before `CronCreate` and skip if already registered.
- [ ] **`/tmp` IS available on Windows git-bash** — CLAUDE.md states "No /tmp on Windows — unreliable in git-bash." The 159d621e session explicitly tested it: `touch /tmp/test-sentinel-check && echo "touch OK"` → succeeded. Subagent agent-a60ab42f9dac9a225 also used `> /tmp/recent_primary.txt` successfully. The rule is stale — update or remove it.
- [ ] **`jq` is not available in git-bash** — Subagent agent-ade629c0f0e8ee14b tried `cat file.jsonl | jq -r ...` multiple times and got RTK Rust panics or silent failures. `jq` is not on the git-bash PATH. Use `node -e` for JSON parsing. Add to Shell: "No `jq` in git-bash — use `node -e` for JSON parsing."
- [ ] **RTK hook over-blocked `bun run test` / `bunx tsc`** — User explicitly said "those bun run should be reachable, please ensure they are reachable." The hook regex was catching test/lint commands needed for pre-handoff checks. Fixed in bc9b038 (exceptions added), but this should be documented: the RTK hook should NEVER block commands that are in the CLAUDE.md pre-handoff checklist.
- [ ] **Transcript parsing: node.js + temp file is the reliable pattern** — Subagents tried jq (Rust panic), python3 (RTK panic), PowerShell (blocked), and grep pipelines (lossy). The working pattern confirmed in the current session is: `tail -N file > $USERPROFILE/AppData/Local/Temp/foo.jsonl && node -e "...fs.readFileSync..."`. Document in Shell or as a snippet in `.agent/workflows/`.

## Memory Updates Needed

- [ ] **Add `feedback_edit_hygiene.md`** — Still missing. Carried over from 2026-05-27. 29 combined "String to replace not found" + "File has not been read" errors. Rule: Read before any Edit; re-read if a prior Edit or format touched the file this session. This is the highest-impact missing memory file.
- [ ] **`project_context.md`** — Dev-server stop command still absent (carried over 3 days): `netstat -ano | grep ":3000" | awk '{print $5}'` + `taskkill //F //PID <n>`. Also: CLAUDE.md `/tmp` rule is stale — update memory to note `/tmp` is usable.
- [ ] **`feedback_shell.md`** — Add: "No `jq` in git-bash — use `node -e` for JSON parsing. No `python3` — same." Currently only mentions the PowerShell ban and RTK prefix rule.
- [ ] **All other memory files** — Reviewed. `feedback_prehandoff_checks.md` and `project_context.md` were updated in bc9b038. No stale content in `feedback_code_style.md`, `feedback_git.md`, `feedback_ui_tasks.md`, `user_profile.md`.

## New Skills / Workflows

- [ ] **Deduplicate cron registration** — The startup hook fires CronCreate unconditionally each session. If multiple sessions run, the same cron accumulates. Pattern fix: before CronCreate, call CronList (once available) and skip if a matching prompt already exists. Until CronList is available, document that the user may see the review prompt 2-3× per morning and this is benign (not a bug to chase).
- [ ] **`dev-server.md` workflow** — ✅ Created in bc9b038. Verify it has the confirmed Windows stop command (`netstat + taskkill`).
- [ ] **`commit-push.md` workflow** — ✅ Created in bc9b038. Verify it includes rtk-prefixed commands.
- [ ] **`format-touched.sh` script** — ✅ Created in bc9b038. `format:touched` and `format:staged` npm scripts added.
- [ ] **JSONL transcript parsing snippet** — The Explore subagent re-derives the parsing approach each time (tries jq, fails, tries python3, fails, falls back to grep). A documented snippet in `.agent/workflows/` would prevent re-derivation: `tail -N file > $USERPROFILE/AppData/Local/Temp/foo.jsonl && node -e "const fs=require('fs'); const lines=fs.readFileSync(path,'utf8').split('\\n')..."`.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — Carry over, 4th day. Still unconfirmed in allowlist.
- [ ] **`Bash(netstat -ano *)`** — Carry over, read-only port check.
- [ ] **Subagents inherit no permissions** — agent-a60ab42f9dac9a225 attempted PowerShell (blocked) and certain Bash commands. Subagents spawned via the Agent tool seem to get a fresh permission context without the parent's allowlist. Worth investigating whether `allowedTools` can be threaded through Agent tool calls.

## Notes

- **10 mobile UI commits in one session (159d621e)** — `Fix mobile workout flow layout`, `Align mobile workout controls`, `Compact mobile dashboard overview`, `Use icons for workout card actions`, `Center mobile exercise filters`, `Improve mobile exercise selector density`, `Use icon workout detail actions`, `Tighten mobile dashboard layouts`, `Compact mobile progress summary cards`, `Centralize mobile bottom nav spacing`. All small, focused commits — good hygiene. No review friction observed in those commits from the transcript tail.
- **Triple-fire nightly review is the highest new friction** — Starting the day with 3 identical review tasks is noisy. The mechanism is the startup hook re-registering the cron each session without checking. Low severity (review still runs), but worth fixing before it multiplies further.
- **`/tmp` rule is definitively stale** — Two independent data points (explicit test + subagent usage) confirm `/tmp` works on git-bash. The note in CLAUDE.md has been causing unnecessary use of `$USERPROFILE/AppData/Local/Temp` which is much more verbose.
- **Subagent JSON-parsing friction pattern** — The Explore subagent agent-ade629c0f0e8ee14b tried jq (multiple Rust panics), python3 (failures), and eventually fell back to grep. The agent-a60ab42f9dac9a225 used `/tmp` + grep and succeeded more cleanly. The node.js pattern (used in the parent session) was fastest; needs to be the documented first-choice.
- **`feedback_edit_hygiene.md` — 4th day unfulfilled** — Flagged 2026-05-27 (second pass), not created in bc9b038 commit. High-impact item that keeps not getting done.
- **Allowlist carry-overs at 4+ days** — `taskkill //F //PID *`, `netstat -ano *`. These are genuinely useful but keep slipping through. Will flag again until actioned.
