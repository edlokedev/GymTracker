# Nightly Review — 2026-07-08

_Transcript scan note: Remote container holds only 1 JSONL (`/root/.claude/projects/-home-user-GymTracker/` — 38 lines, current session only). Historical transcripts from the Windows machine remain unavailable in cloud environment. Analysis based on current-session tool-use trace, git log, repo-state inspection, and carry-over from Jul 7 report._

**HEAD status:** On `main` — NOT detached. First non-detached start in 18 sessions. Local and remote are in sync at `b99b953`. No commits to pull.

**Cron prompt mismatch still active — 18th session:** Live cron trigger still uses old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. First discovery command (`ls ~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/`) produced no output; required `ls ~/.claude/projects/` to locate correct dir. Workflow file on `main` has had the correct path since `7806280`. This is the single most wasteful recurring issue.

**Activity since Jul 7:** No code commits (repo is quiet). No open PRs. All Jul 7 carry-over items remain unresolved.

**Notable: HEAD not detached this session.** Every prior remote session (17 consecutive) started in detached HEAD. This session started on `main`. Cause unknown — may be new container behavior or a CCR harness change. The defensive rule (check `git symbolic-ref HEAD` and reattach) should stay in CLAUDE.md since the root cause is unclear.

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (18 reports, P0)** — §Read First "always use the graph" is still unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (17 of 18 reports, P0)** — Add to §Git: "Remote/cloud sessions frequently start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes." (Updated: 17/18 sessions, not 18/18 — first exception seen today.)

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (7 reports, P0)** — Without the pull, local main lags remote by all commits from prior sessions.

- [ ] **No `.claude/settings.json` exists (18 reports, P0)** — `.claude/` is gitignored. File must be created per-machine/container. No permission allowlist is active; every shell call requires manual approval.

- [ ] **`queryKeys` centralized factory — add to §Architecture (carry-over from Jul 5)** — `src/lib/api/query-keys.ts` exports the only valid cache-key source per ADR-0007. Never construct TanStack Query keys as inline arrays at call sites.

- [ ] **`test/queryWrapper.tsx` mandatory for hook/component tests touching TanStack Query (carry-over from Jul 5)** — `createTestQueryClient()` + fresh `QueryClientProvider` per test; omitting it causes cache bleed between tests.

- [ ] **Feature hook decomposition threshold (carry-over from Jul 5)** — When a feature hook exceeds ~200 lines or manages 3+ independent concerns, split into focused sub-hooks (pattern from `useExerciseLibrary` split in PR #10).

- [ ] **Auth guard inconsistency — two remaining active treatments, do not add a third (18 reports)** — `/workout` dead-end fixed (PR #10). Two remaining patterns: skeleton+LoginPage vs no guard at all. Converge on `LoginPage` render pattern until a shared `RequireAuth` guard is built.

- [ ] **Side effect inside React state updater is a bug — banned pattern (carry-over from Jul 5)** — `useWorkoutSession.ts:518-549`, `useCalendarData.ts:96-113`, `WorkoutSessionManager.tsx:202-292`. Never put network calls, `setState`, or DOM ops inside a `setState` updater function.

- [ ] **Transactionless delete-all-then-insert banned for multi-row writes (carry-over from Jul 5)** — `workout-templates.ts:284-305` can permanently empty a template on mid-write failure. Multi-row mutations must use a SQL RPC or transaction.

- [ ] **URL-rewrite migration pattern (carry-over from Jul 5)** — Dedicated migration file with `UPDATE ... SET url = REPLACE(url, old_prefix, new_prefix)`. Add to §Data.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069) — ALREADY FIXED in code** — `src/lib/auth/index.ts:62` correctly uses `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule still missing from CLAUDE.md to prevent regression.

- [ ] **`docs/handoffs/` absent — day 11** — `c748069` references `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md`; directory doesn't exist. `docs/reviews/` exists — retire or alias.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 16** — Both still at repo root. Correct location: `docs/plans/`. `git mv` needed.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (14 reports)** — `.gitignore` has `server.log` (line 48) and `server.err.log` (line 49) but not `.dev-server.*`. One-line fix.

- [ ] **`status.md` / `decisions.md` in Odin workspace, not repo (16 reports)** — Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage for private routes (16 reports)** — `/api/exercises/custom` and `/api/exercise-favorites` have no `assertUnauthorized` smoke coverage. `PATCH /api/workout-sets` (mutating) also uncovered.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Substitute with isolated `Agent` tool call.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }`

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag is set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — Validate `file.size ≤ 5 MB` in `CustomExerciseForm.tsx`.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check.

- [ ] **`!data.set_order` rejects set_order 0 bug (open, unaddressed)** — `src/routes/api.workout-sets.ts:41` uses `!data.set_order` which rejects valid `set_order: 0`. Fix: `data.set_order == null`.

- [ ] **`parsePaginationParams` shared helper missing (open, unaddressed)** — Pagination parsing is inconsistent. `api.exercises.recent.ts:6-11` clamps 1–50; other route handlers accept NaN/unbounded values.

- [ ] **Body validation: adopt zod pattern for all mutating routes (open, unaddressed)** — `api.exercises.custom.ts` uses zod; other mutating routes blind-cast `await request.json()`. Apply uniformly.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 18th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions.

- [ ] **Graphify stale — 100+ commits since last update** — PRs #4–#11 all unrepresented. Graph is unreliable for current codebase shape.

- [ ] **API surface stale** — `src/lib/api/query-keys.ts` and all TanStack Query hooks are new or migrated; any memory entry tracking the API surface is stale.

- [ ] **Production incident log** — useAuth memoization bug (c748069) consumed ~78% of Vercel Hobby 1M edge-request cap. Add if memory tracks production incidents.

- [ ] **`docs/reviews/` now exists** — `docs/handoffs/` pattern may be superseded. Update any memory entry about review/handoff conventions.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 18th report)** — Live cron still fires with old Windows path. Workflow file on `main` has been correct since `7806280`. Fix: CronDelete old job, CronCreate from `.agent/workflows/nightly-review.md`. Costs 2–3 wasted tool calls per session until fixed.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (P1, 5th report)** — `python3 -c` stdin piping is fragile against the remote JSONL format. Reliable extraction: `grep -o '"command":"[^"]*"'` for Bash inputs, `grep -o '"name":"[^"]*"'` for tool names. Update `.agent/workflows/nightly-review.md`.

- [ ] **`gymmie-verification` skill: add detached HEAD pre-check (defensive, 18th report)** — Step 0: `git symbolic-ref HEAD 2>/dev/null || echo DETACHED`; if detached, `git checkout main && git pull origin main` before proceeding. (HEAD was on main this session — rule still needed as root cause of the exception is unknown.)

- [ ] **`grill-with-docs` workflow (17+ consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md`.

- [ ] **`require-auth-guard` refactor workflow (updated Jul 8)** — 2 inconsistent auth guard patterns remain. A bounded workflow to converge remaining private routes to a shared `RequireAuth` guard.

- [ ] **`query-key-audit` skill** — After TanStack Query migration, verify no inline cache-key arrays remain at call sites. One grep pass: `grep -rn "useQuery\(\[" src/`.

- [ ] **`adapter-hook identity audit` skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror sync (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 18th report** — `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(wc -c ~/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(grep -n*)",
        "Bash(head * ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run; 18th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`grep -o '...' *.jsonl`** — JSONL content extraction.

- [ ] **`grep -n '...' src/...`** — source file spot-checks (e.g., verifying open bugs, gitignore entries).

- [ ] **`wc -l ~/.claude/projects/.../*.jsonl`** — transcript size check before parsing.

---

## Notes

- **Cron path mismatch is the single highest-ROI fix — 18th consecutive session.** The workflow file on `main` has the correct Linux path. The live cron trigger has never been re-created. CronDelete + CronCreate takes ~30 seconds and eliminates the 2–3 wasted discovery tool calls on every nightly run.

- **HEAD NOT detached this session.** This breaks a 17-session streak of detached HEAD starts. Cause unknown. Container may have been provisioned differently, or CCR harness behavior changed. Defensive rule should remain in CLAUDE.md and `gymmie-verification` skill since the pattern is still dominant (17/18 sessions).

- **Repo is quiet.** No code commits since `b99b953` (nightly review Jul 7). No open PRs.

- **All Jul 7 open items remain unresolved.** Nothing was merged or closed since the last review.

- **`!data.set_order` bug still open.** `src/routes/api.workout-sets.ts:41` — falsiness check rejects valid `set_order: 0`. Fix: change to `data.set_order == null`.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 16.** `docs/plans/` is active. One `git mv` fixes this.

- **`.dev-server.log` still not gitignored — 14th report.** `.gitignore` lines 48–49 cover `server.log` and `server.err.log` but not `.dev-server.*`. Absolute lowest-effort open item.

- **`docs/handoffs/` absent — day 11.** `c748069` references a file in a directory that doesn't exist. `docs/reviews/` is the more sensible home.

- **Three uncovered private API routes.** `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` still have no `assertUnauthorized` smoke coverage.

- **`graphify update .` is overdue.** PRs #4–#11 unrepresented in `graphify-out/`. Graph-based navigation is unreliable for current codebase shape.
