# Nightly Review — 2026-05-26

_Two passes today. First pass covered sessions up to 5f9ab66f (modal flash fix). Second pass (this one) adds findings from 159d621e (commit/push + nightly review startup). Items marked ✅ RESOLVED were fixed since first pass._

## Proposed CLAUDE.md Additions

- [ ] **RTK prefix compliance is at ~26%** — 63 unprefixed vs 22 rtk-prefixed commands across transcripts. The written rule has not worked across 3+ days and multiple memory files. Only a mechanical enforcement hook will fix this. (See New Skills/Workflows.)
- [ ] **Dev server stop on Windows is a distinct pattern** — `lsof` is unavailable on git-bash; `kill $(lsof -ti:3000)` silently fails. Confirmed working pattern: `netstat -ano | grep ":3000" | awk '{print $5}'` to get PIDs, then `taskkill //F //PID <n>`. Add to Shell section: reliable Windows dev-server teardown command.
- [ ] **Pre-implementation clarification for UI tasks** — User said "STOP. You're supposed to grill-me before jumping to implementing things and getting it wrong." UI change requests are ambiguous by nature. Add to Style: "UI changes → clarify expected behavior with AskUserQuestion before writing code. Especially: state transitions, empty states, loading states, mobile vs desktop."
- [ ] **Format churn: same files every time** — `CalendarDashboard.test.tsx`, `CalendarDashboard.tsx`, `workout-session/client.ts`, `SetEntry.tsx` appear in most format reverts. After `bun run format`, run `git diff --name-only` and revert any file not in the set touched. ✅ This session: format only touched the 5 expected files — no churn. A `.biome.ignore` or `format:changed` script would eliminate this permanently.
- [ ] **Context overflow signal** — One session hit context limit ("continued from a previous conversation that ran out of context"). Add to Style: "Long tasks (>20 tool calls) → use `/handoff` proactively before context fills."
- [ ] **`bun run format` output reveals churn** — The system-reminder after format lists "files you've previously read" that were modified. Use this as a quick churn-detection signal: if unrelated files appear, revert them immediately.

## Memory Updates Needed

- [ ] **`feedback_prehandoff_checks.md`** — ✅ Accurate and working. Pre-handoff checks ran correctly in latest session without prompting.
- [ ] **`feedback_shell.md`** — ✅ Updated: now correctly states hook BLOCKS (not auto-rewrites). Accurate.
- [ ] **`feedback_ui_tasks.md`** — ✅ Created and accurate.
- [ ] **`project_context.md`** — Add dev-server stop command: `netstat -ano | grep ":3000" | awk '{print $5}'` + `taskkill //F //PID <n>`. Currently missing from all memory files.
- [ ] **`feedback_prehandoff_checks.md`** — Commands listed without `rtk` prefix. Should read `rtk bun run format`, `rtk bun run lint`, etc. to match the shell hard rule.

## New Skills / Workflows

- [ ] **RTK enforcement hook (critical — carry over)** — `PreToolUse` on `Bash` that blocks unprefixed `git`/`bun run`/`bunx` commands. 63 violations in one pass; documentation has failed across 3+ sessions. Hook is the only fix that will actually work. Mirror the PowerShell PreToolUse blocker that was added successfully.
- [ ] **`dev-server.md` workflow** — Start: `rtk bun run dev` as background task. Stop: `netstat -ano | grep ":3000" | awk '{print $5}' | xargs -I{} taskkill //F //PID {}`. Wait-for-ready: `until curl -sf http://localhost:3000 >/dev/null; do sleep 2; done`. All three steps appeared 5+ times across sessions without a standard form.
- [ ] **`format-touched.md` workflow (carry over)** — After `bun run format`, revert churn: `git diff --name-only | grep -vE "<touched-pattern>" | xargs git checkout --`. Or a `scripts/format-touched.sh` that only formats git-staged files.
- [ ] **`commit-push.md` workflow** — Pattern appeared 5+ times: `format → lint → test → build → git add <specific files> → commit → push`. Already partially in `.agent/workflows/commit-push.md` (untracked). Standardize and commit it.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)` — dev server stop** — Appeared 3+ times this cycle. Add to permissions allow: `"Bash(taskkill //F //PID *)"` and `"Bash(taskkill //F //IM node.exe *)"`.
- [ ] **`Bash(netstat -ano *)` — port check** — Read-only, used for PID lookup. Appeared 3+ times. Add: `"Bash(netstat -ano *)"`.
- [ ] **`Bash(rtk bun run format *)`, `Bash(rtk bun run lint *)`** — Not in allowlist; prompt occasionally. Low priority but worth adding.
- [ ] **`Bash(kill *)` / `lsof`** — Both unreliable on Windows git-bash. Standardize on `taskkill` instead; no need to allowlist.

## Notes

- **Highest signal this cycle:** (1) RTK compliance collapsed (~26%) — hook is the only fix, documentation has demonstrably failed. (2) Dev server teardown is fragile on Windows — confirmed `lsof` is unavailable in git-bash, `netstat + taskkill` is the reliable pattern.
- **PowerShell: 7 hits in older transcripts** — all predate the PreToolUse blocker. Zero PowerShell calls in the last two sessions. Hook approach works; RTK needs the same treatment.
- **Pre-handoff checks working:** Latest session ran format/lint/158 tests/build correctly without prompting. Memory file is effective when specific and has clear consequences.
- **Format churn resolved in latest session:** Only 5 expected files touched. Either the rule is being followed, or these files happen to be clean. Watch next session.
- **UI clarification memory is new** — `feedback_ui_tasks.md` was created mid-cycle. Watch whether it prevents the "jump to implementation" pattern next session. One AskUserQuestion call was made but too late (after partial implementation).
- **Cron nightly review re-registered** — job 63cd9b88, fires at 00:07 local. Session-only, expires in 7 days. Re-register at session start via the hook in `nightly-review.md`.
