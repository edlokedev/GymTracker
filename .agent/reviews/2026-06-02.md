# Nightly Review — 2026-06-02

_Sessions scanned: dd2fcb26 (exercise type-aware tracking, NEW), 5f9ab66f (nightly review accumulation), b0b7ba53 (older migration work — mtime bumped but no new lines), agent-a60ab42f9dac9a225 and agent-ade629c0f0e8ee14b (prior review subagents). Note: the second commit "Support explicit workout tracking metrics" (HEAD, 38 files, 2026-06-01 19:36) has no corresponding transcript in the top-5 — either done in a session that wasn't captured or the user committed manually after further edits._

---

## Proposed CLAUDE.md Additions

- [ ] **`find` ban not enforced** — Session dd2fcb26 used `find /c/.../src -name "workout-sets*"` instead of the `Glob` tool. CLAUDE.md says "File search: Use Glob (NOT find or ls)." The rule exists but wasn't followed. Consider adding a PreToolUse hook to block `Bash` commands starting with `find ` (or at least flag them).
- [ ] **`grep`/`rg` via Bash not enforced** — Same session used `rtk grep -rl "..."` twice instead of the `Grep` tool. CLAUDE.md says "Content search: Use Grep (NOT grep or rg)." Add this to the RTK hook exclusions or document more prominently in Style.
- [ ] **`bunx tsc` and `bun run test` without RTK prefix** — Pre-handoff check commands ran as `bunx tsc --noEmit` and `bun run test` (not `rtk`-prefixed). These may be in the hook exceptions added in bc9b038, but the CLAUDE.md Commands section should explicitly list the allowed bare forms to avoid confusion.
- [ ] **`bun run format` and `bun run lint` not visible before first commit** — dd2fcb26 ran `bunx tsc --noEmit` and `bun run test` before committing, but no `bun run format` or `bun run lint` was observed. The pre-handoff checklist requires both. Possible the format/lint steps happened but weren't captured, or they were skipped.
- [ ] **`mcp__ccd_session__mark_chapter` — undocumented MCP tool** — Called once in dd2fcb26 mid-session. Unknown purpose (likely chapter/session segmentation). Not documented anywhere. Add a note to CLAUDE.md or create a reference memory if this is a CCD session management tool.
- [ ] **`.graphifyignore` — new file type** — HEAD commit added `.graphifyignore`. Graphify section in CLAUDE.md documents invocation but not this config file. Add a note: "`.graphifyignore` at project root: files/dirs to exclude from graphify graph."

## Memory Updates Needed

- [ ] **Add `feedback_edit_hygiene.md`** — 7th consecutive day. "String to replace not found" + "File has not been read yet" errors. Rule: Read before Edit; re-read if a prior Edit or format touched the file. High-impact, keeps getting skipped.
- [ ] **`project_context.md`** — Dev-server stop command still missing (7 days). Also: graphify knowledge graph may need updating given the large workout tracking refactor (38-file commit). Check if `graphify-out/` reflects current state.
- [ ] **`feedback_shell.md`** — Add `jq` and `python3` pipe bans to the no-list (not just PowerShell). Also note: `find` and bare `grep/rg` should use dedicated tools per CLAUDE.md.

## New Skills / Workflows

- [ ] **`mcp__ccd_session__mark_chapter` — document usage** — If this is for segmenting long sessions into logical chapters, add it to `.agent/workflows/` so it's used consistently on long feature tasks (like the 38-file refactor).
- [ ] **`graphify update` after large refactor** — The HEAD commit touched 38 files including supabase queries, types, and feature components. CLAUDE.md says "run `graphify update .` after ≥5 files touched." Neither commit shows `graphify update` being called. This likely means the knowledge graph is stale. Add to the commit workflow: "if ≥5 files changed, run `graphify update .` before or after the final commit."

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 7th day carry-over.
- [ ] **`Bash(netstat -ano *)`** — 7th day carry-over.

## Notes

- **`AskUserQuestion` used correctly in dd2fcb26** — Before implementing exercise type-aware tracking, the assistant asked a clarifying question. This confirms `feedback_ui_tasks.md` is working. Positive signal.
- **Graph-first navigation followed** — dd2fcb26 read `graphify-out/wiki/index.md` → `GRAPH_REPORT.md` → source files. The CLAUDE.md rule is being applied.
- **Multiple edits to same file** — `workout-sets.ts` edited 5×, `WorkoutSessionManager.tsx` edited 6× in dd2fcb26. No visible "String to replace not found" errors, but this is the pattern that causes them. The `feedback_edit_hygiene.md` rule would catch this.
- **38-file second commit with no transcript** — The larger "Support explicit workout tracking metrics" commit (38 files, progress/calendar/contracts/queries) has no transcript in the top 5. Either it was done in a separate session that wasn't opened through the project, or changes were applied manually. The graphify graph is likely stale as a result.
- **Cron stacking stable at 4×** — Didn't grow from yesterday. The 4 enqueues/dequeues seen in 5f9ab66f are from the 2026-06-01 review session. Today's session fires the same 4. Not growing further, but still needs dedup fix.
- **Carry-over fatigue** — Items flagged for 7 days (edit hygiene, taskkill permissions, dev-server stop in project_context.md) are clearly not going to self-resolve. Suggest the user action them directly or explicitly defer them so reviews can stop repeating them.
