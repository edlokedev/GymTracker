# Nightly Review — 2026-06-04

_Sessions scanned: 3e7384e3 (PM analysis query on progress dashboard), agent-a2c7106a38f947f7d (subagent reading progress files), 5f9ab66f (nightly review accumulation), 159d621e (schedule setup — no new lines), dd2fcb26 (unchanged). 3 major feature commits on 2026-06-03 (70 files, 5060 insertions) — sessions not in top 5 (closed before review ran)._

---

## Proposed CLAUDE.md Additions

- [ ] **Update Key Hubs list — 3 new features added** — The CLAUDE.md Key Hubs section lists known coupling hotspots. Three new feature areas were introduced in the June 3 commits but aren't listed: `workout-templates` (full feature, new routes/queries/types/tests), `exercise-discovery` (favorites, recent, suggested, quick-picks), `next-workout` planning. Add these to help future sessions orient quickly without a full graph scan.
- [ ] **`graphify update` not called after 70-file commit** — The June 3 feature push touched 70 files across exercise-library, workout-session, workout-templates, exercise-discovery, and routes. CLAUDE.md trigger is "≥5 files touched." No evidence of `graphify update` in any transcript. The knowledge graph is likely stale by at least 2 weeks of feature work. Add explicit note: "After any commit touching ≥5 files across different features, run `graphify update .` before starting the next feature task."
- [ ] **`find` in subagent instead of `Glob` tool** — Subagent `agent-a2c7106a38f947f7d` used `find . -type f -name "*progress*"` as its first search action. Same violation as dd2fcb26 session. The `Glob` preference rule is in CLAUDE.md but subagents don't inherit it. Consider adding the `Glob` rule to the subagent prompt template, or flagging it in the system prompt for Explore subagents.
- [ ] **Remote review still firing 4×** — Yesterday's 5f9ab66f tail shows 4 queue enqueues before today's review. The UserPromptSubmit hook was removed, but the remote schedule via RemoteTrigger may have been registered multiple times (same stacking pattern as CronCreate). Needs investigation: check how many RemoteTrigger registrations exist and deduplicate.

## Memory Updates Needed

- [ ] **Add `feedback_edit_hygiene.md`** — 9th consecutive day. Read before Edit; re-read if prior Edit or format touched the file. This is the most persistent unfulfilled item in the review history.
- [ ] **`project_context.md`** — Significantly stale. Add: (1) dev-server stop command (9 days overdue), (2) 3 new feature areas (workout-templates, exercise-discovery, next-workout), (3) graphify graph is stale — note the last `graphify update` was before the June 1-3 feature sprint. Also: the migration history now has 5 new migration files since the context was written.
- [ ] **`MEMORY.md` index** — Add entry for `feedback_edit_hygiene.md` once created (placeholder in index so it's not forgotten).

## New Skills / Workflows

- [ ] **Product analysis workflow** — The 3e7384e3 session pattern ("act as senior PM, read codebase, give product recommendations") is a reusable workflow: spawn Explore subagent to read relevant feature files + contracts + model types, return structured product analysis. Could be `.agent/workflows/product-analysis.md`.
- [ ] **`graphify update` as post-commit step** — Repeated finding: graphify is always stale after feature sessions. Add to `commit-push.md` workflow: "If ≥5 files touched across features → run `graphify update .` after push." Or add as a separate post-commit hook.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 9th day.
- [ ] **`Bash(netstat -ano *)`** — 9th day.

## Notes

- **Huge feature sprint on June 3** — Three commits, 70 files, 5060 insertions: exercise discovery quick picks (favorites/recent/suggested), history-based set prefill, saved workout templates. New routes (`/workouts`, `/workouts/:id/edit`), 2 new migrations, 7+ new contracts, 10+ new test files. The product is maturing fast — CLAUDE.md knowledge is falling behind.
- **Sessions for June 3 work not captured** — All three feature sessions closed before the review ran. Only the PM analysis session (3e7384e3, 17 lines) and its subagent (35 lines) appear. This is consistent with the pattern where large feature sessions don't appear in the review transcript list.
- **4× remote review stacking** — The hook removal fixed the local CronCreate stacking but the remote schedule (RemoteTrigger) itself may have been registered multiple times. Still getting 4 review prompts per morning.
- **Progress dashboard PM query** — User is now using Claude as a product analyst, not just a coder. The subagent read the full progress feature stack and presumably returned recommendations. This is a good use pattern worth formalizing as a workflow.
- **`feedback_edit_hygiene.md` — 9 days** — At this point, the item exists only in review notes. It needs to be created in the next session that touches memory, not just noted again.
- **Three carry-overs at 9 days**: `feedback_edit_hygiene.md`, `project_context.md` updates, `taskkill/netstat` permissions. Recommend: action these at the start of the next feature session, before writing any code.
