# Nightly Review — 2026-07-06

_Transcript scan note: Remote container holds only 1 JSONL (`~/.claude/projects/-home-user-GymTracker/` — 26 lines at session start, this bootstrap session only). No historical transcripts available in the cloud environment. Analysis based on current-session tool-use trace, git log, carry-over items from Jul 5 report, and repo state inspection._

**HEAD status:** Detached again on session start (16th consecutive occurrence). `git checkout main && git pull origin main` run before this commit. Branch was already up to date; no new commits to fast-forward.

**Cron prompt mismatch still active — 16th session:** Prompt used old Windows path `C--Users-Hii-Documents-Code-GymTracker/`. First two discovery commands failed; required 2 additional calls (`ls ~/.claude/projects/` → `find ~/.claude/ -name "*.jsonl"`) to locate `/root/.claude/projects/-home-user-GymTracker/`. Same failure mode as every prior session.

**Activity since Jul 5:** No code commits. Repo is quiet. All carry-over items from Jul 5 are unchanged.

---

## Proposed CLAUDE.md Additions

### New this report

_(No new patterns surfaced — single bootstrap transcript with no code work.)_

### Escalated carry-overs (count = consecutive reports)

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (16 reports, P0)** — §Read First "always use the graph" still unqualified for remote sessions. Add: "In remote/cloud sessions `graphify-out/` is never present (gitignored) — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start (16 reports, P0)** — Add to §Git: "Remote/cloud sessions always start in detached HEAD. Run `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` BEFORE making any changes."

- [ ] **`git pull origin main` mandatory after `git checkout main` in remote sessions (5 reports, P0)** — Without the pull, any push will conflict with remote commits from prior sessions.

- [ ] **No `.claude/settings.json` exists (16 reports, P0)** — `.claude/` is gitignored. File must be created per-machine/container. No permission allowlist is active; every shell call requires manual approval.

- [ ] **`queryKeys` centralized factory — add to §Architecture (carry-over from Jul 5)** — `src/lib/api/query-keys.ts` exports the only valid cache-key source per ADR-0007. Never construct TanStack Query keys as inline arrays at call sites.

- [ ] **`test/queryWrapper.tsx` mandatory for hook/component tests touching TanStack Query (carry-over from Jul 5)** — `createTestQueryClient()` + fresh `QueryClientProvider` per test; omitting it causes cache bleed between tests.

- [ ] **Feature hook decomposition threshold (carry-over from Jul 5)** — When a feature hook exceeds ~200 lines or manages 3+ independent concerns, split into focused sub-hooks (pattern from `useExerciseLibrary` split in PR #10).

- [ ] **Auth guard inconsistency — three active treatments, do not add a fourth (carry-over from Jul 5)** — Known defect. Converge on `LoginPage` render pattern in `index.tsx:37-39` until a shared `RequireAuth` guard is built.

- [ ] **`/workout` dead-end + `GlobalErrorBoundary` dead code — known-open criticals (carry-over from Jul 5)** — Do not touch these files without resolving the criticals. See Jul 4 frontend fundamentals audit at `docs/reviews/2026-07-04-frontend-fundamentals-audit.md`.

- [ ] **Side effect inside React state updater is a bug — banned pattern (carry-over from Jul 5)** — `useWorkoutSession.ts:518-549`, `useCalendarData.ts:96-113`, `WorkoutSessionManager.tsx:202-292`. Never put network calls, `setState`, or DOM ops inside a `setState` updater function.

- [ ] **Transactionless delete-all-then-insert banned for multi-row writes (carry-over from Jul 5)** — `workout-templates.ts:284-305` can permanently empty a template on mid-write failure. Multi-row mutations must use a SQL RPC or transaction.

- [ ] **URL-rewrite migration pattern (carry-over from Jul 5)** — Dedicated migration file with `UPDATE ... SET url = REPLACE(url, old_prefix, new_prefix)`. Add to §Data.

- [ ] **Object identity from adapter hooks must be memoized (production incident, c748069)** — `useMemo(() => adaptUser(supa.user), [supa.user])` pattern.

- [ ] **`docs/handoffs/` absent — day 9** — `c748069` references a file in a directory that doesn't exist. Resolve: create the directory and commit the doc, or retire the convention in favour of `docs/reviews/`.

- [ ] **PLAN.md / PLAN-REVIEW-LOG.md at root — day 14** — Both still at repo root. Correct location: `docs/plans/`. `git mv` needed.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (12 reports)** — `.gitignore` covers `server.log` and `server.err.log` but not `.dev-server.*`. One-line fix.

- [ ] **`status.md` / `decisions.md` in Odin workspace, not repo (14 reports)** — Add: "Skip when running in remote sessions."

- [ ] **Smoke test coverage for private routes (14 reports)** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` have no `assertUnauthorized` smoke coverage.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Substitute with isolated `Agent` tool call.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — `if (filters.x) { setItems(state.cachedX); setHasMore(false); return; }`

- [ ] **`reflectXIfActive` mutation-aware filter view sync (carry-over)** — Every mutation path must call this helper.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}`, `aria-busy={togglingId === thisId}`.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — `POST/DELETE` returns `new Promise(() => {})` when flag is set.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` from PR #8, undocumented under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations undocumented under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Don't call `supabase.rpc()` directly.

- [ ] **`ConflictError` → 409 in errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect`.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`.

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — Validate `file.size ≤ 5 MB` in `CustomExerciseForm.tsx`.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent wiring behind a mode check.

- [ ] **`!data.set_order` rejects set_order 0 bug (carry-over, open)** — `api.workout-sets.ts:41` — falsiness check rejects valid `set_order: 0`.

- [ ] **`parsePaginationParams` shared helper missing (carry-over, open)** — Pagination parsing is inconsistent; `api.exercises.recent.ts` clamps 1–50, others accept NaN/unbounded.

- [ ] **Body validation: adopt zod pattern for all mutating routes (carry-over, open)** — `api.exercises.custom.ts` uses zod; other mutating routes blind-cast `await request.json()`.

---

## Memory Updates Needed

- [ ] **No `memory/` directory — 16th consecutive report** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit project memory to `.agent/memory/` in-repo, or (b) document that memory audit is permanently skipped in remote sessions.

- [ ] **Graphify stale — 100+ commits since last update** — PRs #4–#11 all unrepresented. Graph is unreliable for current codebase shape.

- [ ] **API surface stale** — `src/lib/api/query-keys.ts` and all TanStack Query hooks are new or migrated; any memory entry tracking the API surface is stale.

- [ ] **Production incident log** — useAuth memoization bug (c748069) consumed ~78% of Vercel Hobby 1M edge-request cap. Add if memory tracks production incidents.

- [ ] **`docs/reviews/` now exists** — `docs/handoffs/` pattern may be superseded. Update any memory entry about review/handoff conventions.

---

## New Skills / Workflows

- [ ] **CRITICAL — re-create cron with corrected prompt (P0, 16th report)** — Live cron still uses old Windows path. Workflow file on `main` has been correct since `7806280`. Fix: CronDelete old job, CronCreate from `.agent/workflows/nightly-review.md`. Costs 4–5 wasted tool calls per session until fixed.

- [ ] **Update nightly-review prompt to use `grep -o` for JSONL parsing (P1, 3rd report)** — `python3 -c` and `node -e` stdin piping both silently return empty output against the remote JSONL format (which contains non-message `type: "queue-operation"` / `type: "deferred_tools_delta"` entries). Reliable extraction: `grep -o '"command":"[^"]*"'` for Bash inputs, `grep -o '"name":"[^"]*"'` for tool names. Update `.agent/workflows/nightly-review.md` accordingly.

- [ ] **`gymmie-verification` skill: add detached HEAD pre-check (16th report)** — Step 0: `git symbolic-ref HEAD`; if detached, `git checkout main && git pull origin main` before proceeding.

- [ ] **`grill-with-docs` workflow (15+ consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md`.

- [ ] **`require-auth-guard` refactor workflow** — 3 inconsistent auth guard patterns across private routes (from Jul 4 audit). A bounded workflow to converge all private routes to a shared `RequireAuth` guard.

- [ ] **`query-key-audit` skill** — After TanStack Query migration, verify no inline cache-key arrays remain at call sites. One grep pass: `grep -rn "useQuery\(\[" src/`.

- [ ] **`adapter-hook identity audit` skill (carry-over from c748069)** — Grep for `useMemo`-less `adaptX(y)` calls in hook bodies.

- [ ] **`.codex/skills/` mirror sync (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. Sync process undocumented.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` — 16th report** — `.claude/` is gitignored; file must be created per-machine/container. Minimum allowlist:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(git pull origin main)",
        "Bash(git branch*)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)",
        "Bash(wc -c ~/.claude/projects/*)",
        "Bash(grep -o*)",
        "Bash(head * ~/.claude/projects/*)",
        "Bash(cat /home/user/GymTracker/.agent/workflows/*)",
        "Bash(cat /home/user/GymTracker/.agent/reviews/*)",
        "Bash(cat /home/user/GymTracker/.gitignore)",
        "Bash(ls /home/user/GymTracker/docs/*)",
        "Bash(ls /home/user/GymTracker/src/features/)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/` and `ls ~/.claude/projects/`** — fire on every nightly run; 16th consecutive.

- [ ] **`git symbolic-ref HEAD`** — read-only HEAD check at session start.

- [ ] **`git checkout main`** and **`git pull origin main`** — needed at session start before every commit.

- [ ] **`grep -o '...' *.jsonl`** — JSONL content extraction; more reliable than python3/node for this format.

- [ ] **`wc -l ~/.claude/projects/.../*.jsonl`** — transcript size check before parsing.

---

## Notes

- **Cron path mismatch is the single highest-ROI fix — 16th consecutive session.** The workflow file on `main` has the correct Linux path. The live cron trigger has not been re-created. CronDelete + CronCreate takes ~30 seconds.

- **Repo is quiet.** No code commits since `2bf9571` (nightly review Jul 5). All open issues carry over unchanged.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 14.** `docs/plans/` is active. One `git mv` fixes this.

- **`.dev-server.log` / `.dev-server.err.log` not gitignored — 12th report.** `.gitignore` has `server.log` and `server.err.log` but not `.dev-server.*`. Lowest-effort open item.

- **Three uncovered private API routes** — `/api/exercises/custom`, `/api/exercise-favorites`, `PATCH /api/workout-sets` still have no `assertUnauthorized` smoke coverage.

- **`graphify update .` is overdue.** PRs #4–#11 unrepresented. Graph-based navigation is unreliable for current codebase shape.

- **`docs/reviews/` exists.** The `docs/handoffs/` convention referenced in `c748069` should be retired in favour of `docs/reviews/` — or `docs/handoffs/` needs to be created and populated.

- **Frontend fundamentals audit criticals (Jul 4) remain unaddressed.** `/workout` auth dead-end and `GlobalErrorBoundary` not registered are the highest-priority unresolved code bugs.
