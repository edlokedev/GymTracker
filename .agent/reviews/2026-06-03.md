# Nightly Review — 2026-06-03

_Sessions scanned: 159d621e (schedule setup + hook removal), 5f9ab66f (nightly review accumulation), dd2fcb26 (exercise type-aware tracking — minor update), b0b7ba53 (unchanged), agent-a60ab42f9dac9a225. New commits: "Fix mobile workout rail actions" + "Harden workout exercise completion flow" (both in workout-session). Major infrastructure change: UserPromptSubmit hook REMOVED, persistent remote schedule via RemoteTrigger activated._

---

## Proposed CLAUDE.md Additions

- [ ] **Nightly review cron stacking: ROOT CAUSE FIXED** — The `UserPromptSubmit` hook that was calling `CronCreate` on every message has been deleted from both project and global `settings.json`. A persistent remote schedule via `RemoteTrigger` (cloud-hosted CCR) now handles the nightly review. This means: (1) no more CronCreate calls on session start, (2) the review survives session restarts, (3) today's 5× firing is the last stacking event. Update `nightly-review.md` to reflect that the schedule is now remote — no local `CronCreate` call needed.
- [ ] **`/schedule` vs `CronCreate` — document the distinction** — The user asked "why is it registered as a nightly session-only cron? how about schedule" — suggesting the session-only vs persistent distinction wasn't clear in `nightly-review.md`. Add to the workflow: "Session-only (`CronCreate`): fires only while this Claude session is open, auto-expires after 7 days. Persistent remote (`/schedule` → `RemoteTrigger`): survives session restarts, runs in Anthropic cloud."
- [ ] **`find` and bare `grep` tool violations persist** — Seen in dd2fcb26 (previous session). The dedicated tool preference (`Glob` over `find`, `Grep` tool over `grep`) is in CLAUDE.md but not enforced. Consider adding to the PreToolUse hook or making the rule more prominent.
- [ ] **`bunx tsc` and `bun run test` without RTK prefix in pre-handoff** — Still occurring in feature sessions. The bc9b038 hook exceptions allow them through, but the CLAUDE.md Commands section should explicitly list the allowed bare forms to avoid confusion about which commands need the prefix.

## Memory Updates Needed

- [ ] **Add `feedback_edit_hygiene.md`** — 8th consecutive day unfulfilled. "String to replace not found" + "File has not been read yet" — read before any Edit, re-read if anything touched the file. This is the longest-standing open item.
- [ ] **`project_context.md`** — Dev-server stop command still missing (8 days). Also flag: the graphify graph may be stale — two feature sessions touched workout-session, progress, and contracts heavily since last `graphify update`.
- [ ] **Update `nightly-review.md`** — The schedule is now remote (RemoteTrigger), not session-only (CronCreate). The workflow doc should reflect this so future sessions don't try to re-register a CronCreate. (Report only — this is a workflow file, not memory or CLAUDE.md.)

## New Skills / Workflows

- [ ] **Remote schedule is now active** — The nightly review runs as a persistent cloud CCR via `/schedule`. No local re-registration needed on session start. The startup hook reminder (previously `UserPromptSubmit` → `CronCreate`) is now gone. Future sessions should NOT call `CronCreate` for nightly review.
- [ ] **`graphify update` after workout-session refactor** — HEAD and HEAD~1 touched 14 files in workout-session (SetEntry, WorkoutSessionManager, model, useWorkoutSession, components.test). CLAUDE.md trigger: ≥5 files touched. No evidence of `graphify update` in transcript. Graph is likely stale.

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 8th day.
- [ ] **`Bash(netstat -ano *)`** — 8th day.

## Notes

- **Cron stacking fully resolved after today** — The `UserPromptSubmit` hook is confirmed deleted (verified in both `C:/Users/Hii/Documents/Code/GymTracker/.claude/settings.json` and `~/.claude/settings.json`). Today's 5× firing is the tail of queued messages from yesterday. Starting next session, the review fires once from the remote schedule.
- **RemoteTrigger is now the correct mechanism** — The user explicitly confirmed this is what they wanted. Any future recommendation to set up recurring agent tasks should use `/schedule` → `RemoteTrigger`, not `CronCreate`.
- **Two workout-session commits (June 2)** — "Fix mobile workout rail actions" (9 files, 879 ins) and "Harden workout exercise completion flow" (5 files, 126 ins). Both focused on WorkoutSessionManager, SetEntry, model, and useWorkoutSession. Tests were added/updated in both. Session transcript for these commits was not in the top 5 (likely a session that closed and rotated out).
- **`feedback_edit_hygiene.md` — 8 days, still missing** — The pattern of noting it and not actioning it suggests it needs to be created in the next session that touches memory files, not deferred again.
- **Carry-over list shrinking** — Cron stacking (RESOLVED), `/tmp` rule update (RESOLVED in 2026-06-01 review notes), `feedback_prehandoff_checks.md` rtk prefix (RESOLVED in bc9b038). Remaining open: `feedback_edit_hygiene.md`, `project_context.md` dev-server stop, `taskkill`/`netstat` permissions. These are the real 3 actionable items.
