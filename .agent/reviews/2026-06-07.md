# Nightly Review — 2026-06-07

_Sessions scanned: 5f9ab66f (today — memory maintenance session, nightly review accumulation), 159d621e (Jun 4 — prior feature + review session), 3e7384e3 (Jun 3 — PM analysis), agent-a2c7106a38f947f7d (Jun 3 subagent), dd2fcb26 (Jun 2 — exercise type-aware tracking). No new code commits since 2026-06-03 (HEAD: 28b611d "Add saved workout management"). Today's session was maintenance-only: created feedback_edit_hygiene.md, updated project_context.md, updated MEMORY.md index._

---

## Proposed CLAUDE.md Additions

- [ ] **Graphify hook warnings ignored — Glob used for feature exploration** — Today's session called `Glob` for `src/features/workout-templates/**/*`, `src/features/exercise-discovery/**/*`, and `src/features/next-workout/**/*` despite the PreToolUse hook warning "graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files." The rule is in CLAUDE.md but the warning is easy to skip past. Consider making the CLAUDE.md wording stronger: "Before calling Glob on any `src/features/` path — check `graphify-out/wiki/index.md` first. Non-negotiable."

- [ ] **Remote review stacking: investigate and deduplicate** — The nightly review prompt fired 3× simultaneously today (Jun 7), same pattern as Jun 4. The RemoteTrigger schedule may have been registered multiple times (same mechanism as the old CronCreate stacking). No investigation has been done on how many remote registrations exist. Add to CLAUDE.md or workflow: "Before registering any new RemoteTrigger schedule, verify no existing schedule for the same prompt is registered."

- [ ] **MEMORY.md had duplicate index entry for 9 days** — `feedback_prehandoff_checks.md` appeared twice in MEMORY.md (last two lines were identical). Fixed today, but implies the index is written append-only without checking for duplicates. Add a note to the memory-writing protocol: "Before adding an entry to MEMORY.md, scan the index for an existing entry by file name."

## Memory Updates Needed

- [ ] **`feedback_edit_hygiene.md` — CREATED TODAY** — 9-day carry-over resolved. No further action needed.
- [ ] **`project_context.md` — UPDATED TODAY** — Dev-server stop command added, 3 new June feature areas documented, graphify stale note added. Memory is now current as of Jun 7.
- [ ] **`MEMORY.md` index — FIXED TODAY** — Duplicate `feedback_prehandoff_checks.md` entry removed, `feedback_edit_hygiene.md` entry added.
- [ ] **`project_context.md` — graphify update still needed** — The stale note was added, but the actual graph hasn't been updated. Note now says "stale as of June 2026 large sprint (70+ files)." Run `graphify update .` from project root to refresh — this is a codebase action, not memory.

## New Skills / Workflows

- [ ] **Memory audit as periodic task** — Today's session revealed a 9-day-old duplicate in MEMORY.md and a missing entry. Suggest adding a monthly (or post-memory-write) mini-audit: scan MEMORY.md for duplicate file references and verify each linked file still exists. Could be a short `.agent/workflows/memory-audit.md`.

- [ ] **RemoteTrigger dedup check** — No documented workflow for verifying/cleaning remote schedule registrations. If `/schedule` is used again, need a way to check existing registrations first. Add to `.agent/workflows/nightly-review.md`: "To check for duplicate remote registrations, inspect the schedule list before adding a new one."

## Permission Prompts to Reduce

- [ ] **`Bash(taskkill //F //PID *)`** and **`Bash(taskkill //F //IM node.exe *)`** — 10th consecutive day. Genuinely useful for dev-server management, low risk. Strong candidate for `permissions.allow`.
- [ ] **`Bash(netstat -ano *)`** — 10th consecutive day. Read-only port/process check. Strong candidate for `permissions.allow`.

## Notes

- **Quiet period since June 3** — No code commits Jun 4–7. Today was maintenance-only. The June 3 sprint (70 files, 5060 insertions: exercise discovery, set prefill, workout templates) is the current HEAD. Progress dashboard PM recommendations from Jun 3 session not yet implemented.
- **All 3 major carry-overs from 10 days of reviews finally actioned** — `feedback_edit_hygiene.md` created, `project_context.md` updated with dev-server stop + new features, MEMORY.md deduped. The remaining true carries are: (1) `taskkill`/`netstat` allowlist additions (user action required), (2) `graphify update .` run (codebase action), (3) remote review stacking investigation.
- **3× stacking persists** — Jun 7 firing was 3× simultaneous. The RemoteTrigger registration count hasn't been verified. This is the highest-friction operational issue still open.
- **Graphify graph stale — 4 days and counting** — The June 3 sprint touched 70+ files. `graphify update .` should be run before the next feature session to ensure graph-first navigation reflects current state.
- **Next feature work** — Based on Jun 3 PM analysis session (3e7384e3), the progress dashboard has an "insight gap" — summarizes data but lacks actionable "so what?" insights. Likely next development target based on user's PM query framing.
