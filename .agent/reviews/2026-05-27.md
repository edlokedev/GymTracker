# Nightly Review — 2026-05-27

_Sessions scanned (most-recent-first by mtime): 5f9ab66f (exercise modal/infinite scroll/filter UI), 159d621e (RTK install + nightly review setup), b0b7ba53 (route deepening + PowerShell era), 8e5896e4 (automated background — no user msgs), 2f76d50f (favicon commit). RTK enforcement hook was installed at end of 159d621e, so 5f9ab66f and b0b7ba53 predate it — unprefixed commands there are expected._

---

## Proposed CLAUDE.md Additions

- [x] **`rm -rf .vercel/output` before production builds** — added to CLAUDE.md pre-handoff step 4.
- [x] **RTK hook blindspot: `SERVER_PRESET=vercel bun run build`** — documented in Shell exceptions; noted env-var prefix bypasses hook.
- [x] **Focused test invocation** — added to CLAUDE.md pre-handoff step 3 and Commands section.
- [x] **`ls src/features/` for discovery** — added note to Graphify section: "use wiki/index.md, not `ls src/features/`".

## Memory Updates Needed

- [x] **`project_context.md`** — added `.vercel/output` cleanup note + updated build command to use rtk prefix.
- [x] **`feedback_prehandoff_checks.md`** — updated checklist: rtk prefixes, `rm -rf .vercel/output`, focused test invocation.
- [ ] **All other memory files** — reviewed and accurate. No stale content found.

## New Skills / Workflows

- [x] **`format-touched.sh` script** — created `scripts/format-touched.sh`. Added `format:touched` and `format:staged` npm scripts to `package.json`.
- [x] **Focused vitest invocation** — documented in CLAUDE.md pre-handoff step 3.

## Permission Prompts to Reduce

- [x] **`bun run test` / `bunx tsc` entries reachable** — added exceptions to RTK hook regex so these bypass the block and reach the permission check.
- [x] **`Bash(rtk git add *)`, `Bash(rtk git commit *)`, `Bash(rtk git push *)`** — added to allowlist.
- [x] **`Bash(rm -rf .vercel/output *)`** — added to allowlist.

## Notes

- **RTK hook is new** — only active from end-of-2026-05-26 onward. Sessions 5f9ab66f and b0b7ba53 predate it; 100% unprefixed commands there are expected. First real test will be next feature session. Watch next review carefully.
- **`format-touched.sh` is the highest-friction unresolved item** — 4 consecutive reviews, zero progress. The manual `git checkout -- <4 files>` revert runs every single session. 15-minute fix that keeps not happening.
- **Session 8e5896e4 (no user messages)** — automated background session, likely a spawned subagent. Failed with `claude: command not found` (Claude CLI not on PATH in subagent context) and `python3: command not found`. Worth investigating whether subagents inherit the correct PATH. The session was looking for "remote-control" skills — unclear trigger.
- **TanStack devtools in production** — user flagged it in b0b7ba53; fixed in commit `2080a57 fix(build): strip development JSX runtime from production bundle`. Resolved.
- **`.vercel/output` cleanup** appeared 6× in b0b7ba53 with no documentation anywhere. This is pure friction — Claude re-discovers it each session.
- **Dead allowlist entries** — the 3 unprefixed entries (`bun run test`, `bun run test *`, `bunx tsc --noEmit *`) are silent dead code post-hook. Not urgent but confusing during debugging.
- **`SERVER_PRESET=vercel bun run build` allowlist vs CLAUDE.md mismatch** — CLAUDE.md says `SERVER_PRESET=vercel rtk bun run build` but allowlist has the bare form. Hook doesn't enforce the rtk prefix here. Low priority but worth aligning.
- **Commits after last agent infra commit (e82629e):** c0fd703, fabbada, 4a10153 — all migration/auth work, no agent infra changes. Good separation.
