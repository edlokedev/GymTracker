# Nightly Review — 2026-06-30

_Transcript scan note: **10th consecutive run** with the Windows path mismatch — `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` does not exist in the remote Linux container. Only 1 JSONL at `~/.claude/projects/-home-user-GymTracker/` — the current bootstrap session (31 lines, this session only). No memory directory accessible. Analysis based on git log since Jun 29, confirmed code delta (none — quiet day), and carry-over items from Jun 29 report._

**HEAD status:** Detached again (`HEAD detached from refs/heads/main`) on session start. Jun 29 noted HEAD as "resolved on main" — that was a snapshot, not a persistent fix. Each fresh remote clone starts in detached HEAD. `git checkout main` required before every commit in remote sessions. This is session-level, not repo-level.

_Activity since Jun 29: **No new code commits.** Only commit since Jun 29 is the nightly review itself (`28aeabf chore(agent): nightly review 2026-06-29`). Repository is quiet; all open items carry forward unchanged._

---

## Proposed CLAUDE.md Additions

### Escalated carry-overs (unresolved — day counts from first report)

- [ ] **Object identity from adapter hooks must be memoized (ESCALATE — production incident, c748069)** — `useAuth()` called `adaptUser()` on every render, building a fresh object each time. Consumers listing `user` in a dep array re-ran every render, driving an infinite edge-request loop (~280k Vercel function invocations/day, ~78% of Hobby cap). Fix: `useMemo(() => adaptUser(supa.user), [supa.user])`. Rule: any hook that calls an adapter or constructor to transform context data must memoize its output on the stable source reference. Check: `adaptX(y)` or `new X(y.field)` in a hook body where the return is not wrapped in `useMemo` → flag.

- [ ] **Client-side filter short-circuit pattern (carry-over)** — When the full dataset is already in memory, `runSearch` can short-circuit the API call and serve from in-memory state. Shape: `if (filters.favourites) { setExercises(state.favoriteExercises); setTotal(n); setHasMore(false); return; }`. Only when: client has the full set and the filter is a view, not a new query.

- [ ] **`reflectFavouritesIfActive` — mutation-aware filter view sync (carry-over)** — Every mutation path (toggle success, refresh) must call a `reflectXIfActive(newSet)` helper to update the displayed list. Without this, the filter view goes stale after mutation.

- [ ] **Global disable-while-any-pending for concurrent mutation protection (carry-over)** — `disabled={togglingId != null}` on every control, `aria-busy={togglingId === thisId}` on the active one.

- [ ] **Mode-gating auth features in reused components (carry-over)** — Gate auth-dependent UI wiring behind a mode check when a component is used in multiple contexts.

- [ ] **`vi.hoisted` mutable state for per-test auth control (carry-over)** — `const authState = vi.hoisted(() => ({ user: null as { id: string } | null }))`. Mutate per-test inside `vi.mock`.

- [ ] **`pendingMutation: true` fetch stub for in-flight UI assertions (carry-over)** — POST/DELETE returns `new Promise(() => {})` when `pendingMutation: true` is passed to the fetch stub.

- [ ] **Vercel Analytics + Speed Insights placement (carry-over)** — Mount in `__root.tsx` body, outside `AuthProvider`, so unauthenticated routes are also tracked.

- [ ] **`docs/handoffs/` directory referenced but not committed (ESCALATE — day 4)** — `docs/handoffs/2026-06-27-useauth-memo-edge-request-runaway.md` referenced in commit `c748069` does not exist. Commit the doc or drop the convention. `docs/handoffs/` absent confirmed today.

- [ ] **`graphify-out/` absent in remote — skip graph step in cloud sessions (ESCALATE — 10 reports, P0)** — Still not added to CLAUDE.md. The §Read First "always use the graph" instruction wastes tool calls on every remote run. Add: "In remote/cloud sessions `graphify-out/` is never present — skip to `rg` search immediately."

- [ ] **Detached HEAD on every remote session start — checkout main before committing (ESCALATE — 10 reports, re-confirmed today)** — Jun 29 marked this "resolved" but HEAD was detached again today. Remote clones always start detached; Jun 29 was coincidental. Add to §Git: "Remote/cloud sessions start in detached HEAD. Run `git symbolic-ref HEAD` to verify; if detached, `git checkout main` before any commit."

- [ ] **`status.md` / `decisions.md` location (ESCALATE — 8 reports)** — These live in the Odin workspace root, not in GymTracker/. Add: "Skip when running in remote sessions."

- [ ] **Plan file placement convention — PLAN.md / PLAN-REVIEW-LOG.md still at root (ESCALATE — day 8)** — Both files confirmed at `/home/user/GymTracker/PLAN.md` and `/home/user/GymTracker/PLAN-REVIEW-LOG.md`. Correct location: `docs/plans/custom-exercises.md` and `docs/plans/custom-exercises-PLAN-REVIEW-LOG.md`. 2-minute commit.

- [ ] **Smoke test coverage rule for private routes (ESCALATE — 8 reports)** — `/api/exercises/custom` and `/api/exercise-favorites` have no `assertUnauthorized` smoke coverage. Add to §Testing: every new `privateMethod` route needs `assertUnauthorized` before merge.

- [ ] **Custom SQLSTATE GYM namespace (carry-over)** — `GYM01`–`GYM04` error codes from PR #8 undocumented. Add under §Architecture.

- [ ] **SECURITY INVOKER RPC + row-lock pattern (carry-over)** — Multi-row atomic mutations via SECURITY INVOKER functions undocumented. Add under §Architecture.

- [ ] **Optimistic saved/unsaved session branch in `useWorkoutSession` (carry-over)** — New commands must handle both branches. Add under §Architecture.

- [ ] **`queryClient.rpc<T>()` typed wrapper (carry-over)** — Do not call `supabase.rpc()` directly. Add under §Architecture.

- [ ] **`ConflictError` → 409 added to errors.ts (carry-over)** — Add to §API + Auth typed-error catalogue.

- [ ] **Ref-guard one-time seed pattern (carry-over Jun 23+)** — `useRef<SomeId | null>(null)` guard in `useEffect` for one-time seeds.

- [ ] **`async fn → void prop` call-site wrapping (carry-over Jun 23+)** — `onFoo={() => { void modelFn() }}`. Add under §API + Auth or Testing.

- [ ] **`.dev-server.log` / `.dev-server.err.log` not gitignored (ESCALATE — 7 reports, confirmed today)** — `.gitignore` has `server.log`/`server.err.log` but no `.dev-server.*` entries. Add both patterns.

- [ ] **Codex CLI unavailable in remote (carry-over Jun 21+)** — Add to §Shell: "In remote sessions substitute Codex Act 2 with an isolated-context `Agent` tool call."

- [ ] **Image upload file size validation missing (carry-over Jun 22+)** — `CustomExerciseForm.tsx` validates MIME type but not file size. Add rule: validate `file.size ≤ 5 MB` client-side before `uploadCustomExerciseImage`.

---

## Memory Updates Needed

- [ ] **No `memory/` directory accessible (10th consecutive report)** — `~/.claude/projects/-home-user-GymTracker/memory/` does not exist. Decide: (a) commit memory to `.agent/memory/` in-repo, or (b) document that memory audit is skipped in remote sessions. Each run that revisits this wastes overhead.

- [ ] **Graphify stale — 90+ commits since last update** — PR #4 through PR #9 plus post-merge commits. Any memory entry marking the graph as current is stale.

- [ ] **Production incident log** — useAuth memoization bug consumed ~78% of Vercel Hobby 1M edge-request cap in a single day. If memory tracks production incidents, add this.

- [ ] **New routes/API surface since last graph update** — `/api/exercise-favorites` (GET/POST/DELETE) added in PR #9. Any memory entry tracking the API surface is stale.

---

## New Skills / Workflows

- [ ] **`grill-with-docs` workflow (ESCALATE — 10 consecutive reports, P0)** — Confirmed used on 4+ shipped features. Create `.agent/workflows/grill-with-docs.md` before the next feature's plan phase. Without it the process is rediscovered from scratch each time.

- [ ] **Fix nightly-review workflow prompt path (ESCALATE — 10 reports, P0, one-line fix)** — `.agent/workflows/nightly-review.md` line 20 hardcodes `~/.claude/projects/C--Users-Hii-Documents-Code-GymTracker/` which has **never resolved** in 10 consecutive remote runs. Correct path: `~/.claude/projects/-home-user-GymTracker/`. This is the single highest-ROI change in the entire backlog — one edit, eliminates 4–6 wasted tool calls on every run.

- [ ] **Adapter-hook identity audit skill (carry-over — from c748069)** — Grep for `useMemo`-less `adaptX(y)` or `new X(y.field)` calls in hook bodies to catch infinite-loop risks before they ship.

- [ ] **`docs/reviews/` vs `docs/plans/` vs `.agent/reviews/` naming clarification (carry-over)** — `docs/plans/` = feature plans + grill-with-docs review logs. `.agent/reviews/` = nightly self-reflection. Update `.agent/workflows/nightly-review.md` with this distinction.

- [ ] **`.codex/skills/` mirror purpose undocumented (carry-over Jun 14+)** — `.codex/skills/gymmie-verification/` mirrors `.agent/skills/gymmie-verification/`. The sync process and whether new skills must be mirrored are undocumented. Confirmed present today.

---

## Permission Prompts to Reduce

- [ ] **No `.claude/settings.json` exists in the project (ESCALATE — 10 reports)** — `permissions.allow` has never been configured. All Bash commands go through prompts. Create `.claude/settings.json` with at minimum:
  ```json
  {
    "permissions": {
      "allow": [
        "Bash(find ~/.claude/projects/* -name \"*.jsonl\")",
        "Bash(ls ~/.claude/projects/*)",
        "Bash(ls ~/.claude/projects/-home-user-GymTracker/*)",
        "Bash(git log*)",
        "Bash(git status*)",
        "Bash(git diff*)",
        "Bash(git show*)",
        "Bash(git symbolic-ref HEAD)",
        "Bash(git checkout main)",
        "Bash(ls /home/user/GymTracker/.agent/*)",
        "Bash(wc -l ~/.claude/projects/*)"
      ]
    }
  }
  ```

- [ ] **`find ~/.claude/projects/`** and **`ls ~/.claude/projects/`** — Fire on every nightly review run (10th consecutive). Highest-priority candidates.

- [ ] **`git symbolic-ref HEAD`** — Read-only HEAD check run at session start. Should be in the allowlist.

- [ ] **`git checkout main`** — Needed on every remote session to detach from detached HEAD before committing. Read-only in effect (no file changes); low-risk candidate.

- [ ] **`taskkill` and `netstat` (Windows, carry-over)** — `Bash(taskkill //F //IM node.exe*)`, `Bash(netstat -ano*)`.

---

## Notes

- **No new code activity today.** Only commit since Jun 29 is the nightly review itself. Quiet day; all open items carry forward.

- **Detached HEAD recurred (10th report, contradiction of Jun 29 "resolved").** Jun 29 observed HEAD on `main` and called it resolved, but it was a coincidence — remote clones always start in detached HEAD. Today confirmed detached again. Rule in §Git is still missing. `git checkout main` was run successfully in this session before committing.

- **CRITICAL — fix the nightly-review path (10th report, P0).** Editing line 20 of `.agent/workflows/nightly-review.md` from `C--Users-Hii-Documents-Code-GymTracker/` to `-home-user-GymTracker/` is a single-line change. It would eliminate wasted `ls` and `find` calls on every future run. This has been flagged for 10 consecutive runs. If the nightly prompt is the trigger, the prompt in the task trigger also needs to be updated.

- **PLAN.md / PLAN-REVIEW-LOG.md at root — day 8.** Confirmed at `/home/user/GymTracker/PLAN.md` and `/home/user/GymTracker/PLAN-REVIEW-LOG.md`. `docs/plans/` already exists and contains active plan files. 2-minute commit.

- **`docs/handoffs/` absent — day 4.** `docs/` exists with `adrs/`, `plans/`, `reviews/`, `specs/`, `stories/`, `deployment/`. No `handoffs/` subdirectory. The commit message `c748069` references a file in this directory that doesn't exist.

- **`.dev-server.log` not gitignored — confirmed today.** `grep "dev-server" .gitignore` returns nothing. If the dev server writes these files, they'll appear as untracked noise.

- **`grill-with-docs` workflow unwritten — 10 flags.** It is demonstrably established practice (evidenced by `docs/plans/favourites-toggle-PLAN-REVIEW-LOG.md`). Workflow doc must exist before the next feature plan phase.

- **Three uncovered private API routes.** `/api/exercises/custom`, `/api/exercise-favorites`, and `PATCH /api/workout-sets` still have no `assertUnauthorized` smoke coverage. A RLS regression on any could ship through CI silently.

- **Vercel Analytics added `a9e3dbc` — now live.** Worth revisiting open perf items (P6–P9, P12: ilike/FTS, suggested-exercises scan, unbounded `listRecentRows`, N+1 category counts, per-request auth round-trip) after a week of real production traces.
