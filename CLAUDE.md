<!-- generated-by: .odin/scripts/sync-instructions.sh -->
# Claude Instructions

This file is generated for Claude and Claude Code from `projects/gymmie/AGENTS.md`.

Do not edit this file directly. Edit the local `AGENTS.md`, then run from the Odin root:

```bash
bash .odin/scripts/sync-instructions.sh
# fallback if Git Bash is unavailable
bun run .odin/scripts/sync-instructions.js
```

Use Claude-specific tool behavior when available, but follow the canonical rules below.

## Canonical Instructions

# Gymmie — Agent Instructions

Gymmie is a mobile-first workout tracker. This file is the **canonical, cross-agent instruction source** for the project — `CLAUDE.md` and `GEMINI.md` are generated from it via `bash .odin/scripts/sync-instructions.sh`. Do not hand-edit the generated files.

This is also an Odin project (migrated 2026-06-15). The root `AGENTS.md` is the higher-level workspace policy; this file governs in-repo work. The project's `.git` is independent (its own GitHub remote); Odin itself is not a git repo.

## Read First

1. Root `AGENTS.md` — workspace policy.
2. This file.
3. `CONTEXT.md` — domain language.
4. `project.yaml`, `status.md`, `decisions.md` — project state; `docs/adrs/` for past decisions.
5. Code nav/understanding → `graphify-out/wiki/index.md`, then `graphify-out/GRAPH_REPORT.md`. Raw `rg` only when the graph doesn't resolve it. Verify graph findings against source; never hand-edit `graphify-out`.
6. Library-specific code → Context7 first, else official docs.

## Style

Caveman mode, always: drop articles, filler, pleasantries, hedging. Fragments OK. Abbreviate DB/auth/config/req/res/fn/impl. Technical terms exact; code blocks unchanged.

- Direct impl after reading the codebase.
- Don't revert user changes unless asked.
- Small, reviewable commit slices.
- Ambiguous task → inspect repo/docs first, ask only the blocking question.
- **UI tasks → clarify with AskUserQuestion before writing code.** State transitions, empty states, loading states, mobile behavior — ask first. Never assume.

## Architecture

- Stack: Vite + TanStack Start/Router + React 19 + Tailwind v4 (`@tailwindcss/vite`) + Bun.
- Target: Vercel + Supabase (Postgres + Supabase Auth + RLS). Not Zo Sites — rejected because it would require a SQLite→Postgres rewrite, TanStack Start removal, a Hono API, and auth re-adaptation.
- API responses use a shared envelope unless the route is a redirect.
- Private routes derive the authed user server-side. Never trust `userId` from query/body. Supabase RLS is the auth boundary for private tables.
- Exercise media: SELF-HOSTED in our own public repo `edlokedev/gymmie-exercise-media`, served via jsDelivr pinned to a COMMIT HASH (currently `19268b2`); never repoint to a mutable ref. History: original upstream purged all media 2026-06-30 (ownership dispute, issue #0013); preserved via a pre-purge fork. Backups: NAS `N:\TOBESORTED\exercises-dataset-media-archive` + local `~/code/gymmie-exercise-media`. Dataset JSON for reseeds still reads the pinned fork snapshot. Images/GIFs are not committed to this repo.

Docs: `CONTEXT.md` · `docs/supabase-vercel-migration-plan.md` · `docs/adrs/` · `graphify-out/GRAPH_REPORT.md`.

## Source Layout

- `src/app/components` — app shell (header, error boundary)
- `src/components/ui` — shared UI primitives
- `src/features/*` — feature-first UI/model/client
- `src/lib/api` — envelopes, route helpers, error handling
- `src/lib/supabase` — clients, generated types, seed helpers
- `src/routes` — TanStack route files, server/API boundaries
- `supabase/migrations` — Postgres schema + RLS
- `scripts` — smoke + ops scripts

## Graphify

Graph lives in `graphify-out/`. **Always use the graph before raw search.**

- `wiki/index.md` → entry point for file/feature lookup (use this, not `ls src/features/`).
- `GRAPH_REPORT.md` → god nodes, communities, coupling hotspots.

Run `graphify update .` after: a new or significantly restructured feature · a route/API shape change · an auth or DB layer change · ≥5 files touched in one task.

## Styling

Read `.agent/skills/styling/SKILL.md` before touching UI.

- **Mobile-first** — gym use is on mobile. Touch targets ≥ 44px.
- Tailwind utilities inline; extract only when reused ≥3×.
- Static class strings only — no dynamic Tailwind construction.
- Clickable things look and behave clickable (`cursor-pointer`).
- Preserve gradient direction unless asked to remove.
- No nested cards or oversized marketing layouts on dashboard surfaces.

## Shell — Hard Rules

See `.agent/skills/windows-shell/SKILL.md`. Summary:

- **Bash tool only. The PowerShell tool is banned** (a hook blocks it).
- **Every command uses the `rtk` prefix** (hook-enforced): `rtk git status`, `rtk bun run test`, `rtk bunx tsc --noEmit`. Unprefixed `git` / `bun run` / `bunx` is blocked.
- Exceptions (no rtk): `bun install`, `bun add`, `bun remove`, `bun --version`, `bun run test`, `bunx tsc`.
- `SERVER_PRESET=vercel bun run build` works bare or rtk-prefixed (the env-var prefix bypasses the hook).
- If `rtk: command not found` → `export PATH="$USERPROFILE/bin:$PATH"`, then retry.
- No `/tmp` on Windows — pipe via `process.stdin`, or use `$USERPROFILE/AppData/Local/Temp`.
- No `python` / `python3` — use `node -e`. Stdin pattern: `cmd | node -e "const c=[];process.stdin.on('data',d=>c.push(d));process.stdin.on('end',()=>{const d=JSON.parse(Buffer.concat(c).toString()); /* ... */ })"`.
- Stop dev server (Windows): `taskkill //F //IM node.exe 2>/dev/null; true`. Don't use `kill $(lsof -ti:3000)`.
- node_modules + build artifacts are per-machine disposables — never synced into Odin. Don't run installs or the dev server against a slow or actively-syncing copy; use a local checkout. Git on the synced folder needs `git config --global --add safe.directory '*'` per machine.

## Commands

```bash
rtk bun run dev | format | lint | test | build | smoke
```

Pre-handoff after code/config changes (or run the `gymmie-verification` skill):

1. `rtk bun run format` → `rtk git diff --name-only`, then revert churn: `rtk git checkout -- <unrelated-files>`
2. `rtk bun run lint`
3. Focused tests for touched behavior: `bun run test src/features/<feature>/` or `bunx vitest run <file>`
4. `rm -rf .vercel/output 2>/dev/null; true` — clear stale Vercel build artifacts
5. `SERVER_PRESET=vercel rtk bun run build`
6. `rtk bun run smoke` if routing/API changed

Can't run a check → say why. Report lint warnings, don't hide them.

## Git

- `rtk git status --short --branch` first.
- No destructive git ops without explicit request.
- Migration work on a branch separate from `master`.
- Coherent commit slices.

## Data

- No dirty SQLite DB uploads to prod. No mock users in prod.
- Exercise seed: explicit and repeatable.
- Local DB files stay gitignored.

## API + Auth

- Preserve route response shapes during migration unless a contract change is intentional.
- Contract tests for route req/res behavior.
- Private workout data scoped by the authed user server-side; RLS is the boundary for private tables.

## Testing

- Data/auth/routing changes → focused tests before broad refactors.
- UI changes → key workflows + mobile layout.
- Smoke tests for route/API regressions.

## Odin Integration

- Update `status.md` / `decisions.md` when meaningful project state changes.
- Graph outputs are maps, not source truth.
- Software/architecture decisions → `docs/adrs/`. Odin-operational notes → Odin `memory/`.

